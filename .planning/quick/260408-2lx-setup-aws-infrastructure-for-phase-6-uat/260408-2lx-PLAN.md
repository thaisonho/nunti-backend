---
phase: quick
plan: 260408-2lx
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/infra/setup-oidc-s3.sh
  - .github/workflows/release-deploy.yml
  - .github/workflows/release-promote.yml
  - .github/workflows/release-rollback.yml
  - docs/deployment/environment-configs.md
autonomous: true
requirements: [UAT-AWS-INFRA]
must_haves:
  truths:
    - GitHub Actions authenticates to AWS via OIDC
    - S3 bucket for deployment artifacts is configured
    - Required infrastructure secrets/variables are documented
  artifacts:
    - path: scripts/infra/setup-oidc-s3.sh
      provides: Infrastructure initialization script
    - path: docs/deployment/environment-configs.md
      provides: Documentation for environment variables and secrets
  key_links:
    - from: .github/workflows/release-deploy.yml
      to: AWS
      via: aws-actions/configure-aws-credentials using OIDC
---

<objective>
Setup AWS infrastructure for phase 6 UAT by establishing OIDC authentication for GitHub Actions, creating scripts for IAM Role/S3 bucket setup, and documenting required variables.

Purpose: Move away from long-lived AWS credentials to secure OIDC federation and standardize the infrastructure setup needed for UAT and production.
Output: Bash script for AWS scaffolding, updated GH Actions workflows, and documentation.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create infrastructure setup script</name>
  <files>scripts/infra/setup-oidc-s3.sh</files>
  <action>
    Create a bash script `setup-oidc-s3.sh` that uses AWS CLI to:
    1. Create an S3 bucket for SAM deployment artifacts (with versioning, block public access).
    2. Setup GitHub OIDC Identity Provider in AWS IAM if it doesn't exist.
    3. Create an IAM Role for GitHub Actions that trusts the OIDC provider for this repository.
    4. Attach necessary minimal policies to the role for SAM deployment (S3, CloudFormation, IAM, API Gateway, DynamoDB access etc).
    Make the script executable (+x). Handle existing resources gracefully.
  </action>
  <verify>
    <automated>bash -n scripts/infra/setup-oidc-s3.sh</automated>
  </verify>
  <done>Script exists, has execution permissions, and is syntactically valid bash.</done>
</task>

<task type="auto">
  <name>Task 2: Update GitHub Actions to use OIDC</name>
  <files>.github/workflows/release-deploy.yml, .github/workflows/release-promote.yml, .github/workflows/release-rollback.yml</files>
  <action>
    Update the AWS credential configuration in deployment workflows:
    1. Add `permissions:` block with `id-token: write` and `contents: read` to the workflow jobs.
    2. Remove `aws-access-key-id` and `aws-secret-access-key` from AWS configure actions.
    3. Add `role-to-assume: ${{ secrets.AWS_OIDC_ROLE_ARN }}` and retain `aws-region: ${{ vars.AWS_REGION }}`.
  </action>
  <verify>
    <automated>cat .github/workflows/release-deploy.yml | grep -q "role-to-assume"</automated>
  </verify>
  <done>Workflows are updated to use OIDC role assumption instead of static credentials.</done>
</task>

<task type="auto">
  <name>Task 3: Document Required Environment Variables and Secrets</name>
  <files>docs/deployment/environment-configs.md</files>
  <action>
    Create documentation detailing the Environment Variables and GitHub Secrets required for the CI/CD pipeline to function correctly:
    1. Define GitHub Variables (e.g., `AWS_REGION`).
    2. Define GitHub Secrets (e.g., `AWS_OIDC_ROLE_ARN`).
    3. Explain how to run `setup-oidc-s3.sh` and populate these values in GitHub Settings.
  </action>
  <verify>
    <automated>cat docs/deployment/environment-configs.md | grep -q "AWS_OIDC_ROLE_ARN"</automated>
  </verify>
  <done>Documentation clearly enumerates all required variables and secrets for GitHub Actions.</done>
</task>

</tasks>

<verification>
- `setup-oidc-s3.sh` validation passes
- OIDC permissions correctly added to github actions
- Documentation includes references to OIDC roles
</verification>

<success_criteria>
Workflows are modernized for OIDC, setup script is available to scaffold the required infrastructure, and everything is thoroughly documented for operators.
</success_criteria>

<output>
After completion, create `.planning/quick/260408-2lx-setup-aws-infrastructure-for-phase-6-uat/260408-2lx-SUMMARY.md`
</output>