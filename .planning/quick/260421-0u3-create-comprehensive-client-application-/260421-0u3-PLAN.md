---
phase: quick/260421-0u3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [docs/client-application-development.md]
autonomous: true
requirements: [QUICK-DOC-01]

must_haves:
  truths:
    - "Client developers can understand complete integration flow from signup to message delivery"
    - "Client developers can handle errors and implement retry strategies"
    - "Client developers can secure tokens and E2EE keys properly"
    - "Client developers can troubleshoot common integration issues"
    - "Client developers can implement the integration in multiple languages/frameworks"
  artifacts:
    - path: "docs/client-application-development.md"
      provides: "Comprehensive client integration guide"
      min_lines: 800
      contains: ["Error Handling", "Security Best Practices", "Code Examples", "Troubleshooting"]
  key_links:
    - from: "Authentication flow"
      to: "Device registration"
      via: "Token usage pattern"
      pattern: "accessToken.*devices/register"
    - from: "HTTP endpoints"
      to: "WebSocket connection"
      via: "Auth header consistency"
      pattern: "Authorization.*Bearer"
---

<objective>
Expand docs/client-application-development.md from a simple 174-line guide into a comprehensive, production-ready client integration reference.

Purpose: Enable frontend/mobile developers to integrate with the backend confidently, handling all edge cases, security concerns, and implementation patterns across multiple platforms.

Output: Production-grade documentation covering authentication, device management, messaging flows, error handling, security best practices, multi-language examples, troubleshooting, and testing strategies.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@docs/client-application-development.md

<interfaces>
<!-- Key types and patterns from the backend that clients integrate with -->

From src/auth/cognito-service.ts:
```typescript
export interface SignUpInput {
  email: string;
  password: string;
}

export interface SignInInput {
  email: string;
  password: string;
}

export interface SignInResult {
  accessToken: string;
  refreshToken: string;
  idToken: string;
}
```

From src/messages/message-model.ts:
```typescript
export interface DirectMessageRequest {
  action: 'sendMessage';
  messageId: string;
  recipientUserId: string;
  recipientDeviceId: string;
  ciphertext: string;
}

export interface DirectMessageEvent {
  eventType: 'direct-message';
  messageId: string;
  senderUserId: string;
  senderDeviceId: string;
  ciphertext: string;
  timestamp: string;
}

export interface DeliveryStatusEvent {
  eventType: 'delivery-status';
  messageId: string;
  status: 'delivered' | 'accepted-queued' | 'failed';
  timestamp: string;
}
```

From src/messages/group-message-model.ts:
```typescript
export interface GroupSendRequest {
  action: 'groupSend';
  groupMessageId: string;
  groupId: string;
  ciphertext: string;
  attachments?: AttachmentEnvelope[];
}

export interface AttachmentEnvelope {
  attachmentId: string;
  mimeType: string;
  sizeBytes: number;
  encryptedKey: string;
  s3Key: string;
}
```

From src/app/http-response.ts:
```typescript
export interface SuccessResponse<T = unknown> {
  data: T;
  requestId: string;
}

export interface ErrorResponseBody {
  error: {
    code: string;
    message: string;
    requestId: string;
  };
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand client documentation with comprehensive integration guide</name>
  <files>docs/client-application-development.md</files>
  <action>
Expand the existing 174-line simple guide into a comprehensive 800+ line production-ready reference. Preserve the existing structure and content, but significantly expand each section with:

**1. Enhanced Configuration Section:**
- Add explanation of why each config variable is needed
- Add security notes (never expose client secret, token storage best practices)
- Add environment-specific configuration examples (dev/staging/prod)

**2. Detailed Authentication Flow:**
- Expand signup/signin with complete request/response examples
- Add token refresh flow with code examples
- Add token expiration handling patterns
- Add session management best practices

**3. Comprehensive Error Handling:**
- Add new section "Error Handling Patterns" covering:
  - HTTP error codes and their meanings (400, 401, 403, 404, 409, 500, 503)
  - WebSocket error event handling
  - Network failure scenarios
  - Retry strategies with exponential backoff examples
  - Circuit breaker pattern for degraded service
  - Idempotency key usage to prevent duplicate sends

**4. Security Best Practices:**
- Add new section "Security Best Practices" covering:
  - Token storage (secure storage APIs, never localStorage for sensitive tokens)
  - E2EE key management (device-specific keys, secure enclave usage)
  - Certificate pinning recommendations
  - Input validation before encryption
  - Secure deletion of keys on device revocation
  - Protection against replay attacks

**5. Multi-Language Code Examples:**
- Add new section "Complete Integration Examples" with:
  - JavaScript/TypeScript (fetch API + WebSocket)
  - React Native example (AsyncStorage + WebSocket)
  - Swift/iOS example (URLSession + Starscream WebSocket)
  - Kotlin/Android example (OkHttp + WebSocket)
- Each example should show: auth flow, device registration, sending message, receiving message

**6. WebSocket Connection Management:**
- Expand WebSocket section with:
  - Connection lifecycle (connect, authenticate, maintain, reconnect)
  - Heartbeat/ping-pong pattern
  - Reconnection strategy with exponential backoff
  - Handling connection state transitions
  - Queue management during offline periods

**7. Troubleshooting Guide:**
- Add new section "Troubleshooting Common Issues" covering:
  - "401 Unauthorized" - token expired or invalid
  - "403 Forbidden" - device not trusted or revoked
  - "WebSocket connection fails" - auth header format, token issues
  - "Messages not delivered" - recipient offline, invalid deviceId
  - "Duplicate messages" - idempotency key reuse
  - Debug logging recommendations (what to log, what to redact)

**8. Testing Strategies:**
- Add new section "Testing Your Integration" covering:
  - Unit testing auth flows with mocked responses
  - Integration testing with test accounts
  - WebSocket connection testing
  - End-to-end message delivery testing
  - Load testing considerations
  - Monitoring and observability (what metrics to track)

**9. Performance Optimization:**
- Add new section "Performance Best Practices" covering:
  - Connection pooling for HTTP requests
  - WebSocket connection reuse
  - Message batching strategies
  - Pagination for device/message lists
  - Caching strategies for user/device metadata

**10. Migration and Versioning:**
- Add new section "API Versioning and Migration" covering:
  - Current API version (/v1/)
  - Backward compatibility guarantees
  - Deprecation policy
  - How to handle breaking changes

Maintain the developer-friendly, practical tone. Use tables, code blocks, and clear examples throughout. Target 800-1000 lines total.
  </action>
  <verify>
    <automated>wc -l docs/client-application-development.md | awk '{print $1}' | grep -E '^[8-9][0-9]{2}|^[1-9][0-9]{3}$'</automated>
  </verify>
  <done>Documentation expanded to 800+ lines with comprehensive coverage of error handling, security, multi-language examples, troubleshooting, testing, and performance optimization while preserving original structure</done>
</task>

</tasks>

<verification>
- Documentation file exists and is 800+ lines
- All new sections present: Error Handling, Security Best Practices, Code Examples, Troubleshooting, Testing, Performance, Migration
- Code examples provided for at least 3 different languages/frameworks
- Original simple guide content preserved and enhanced
- Practical, actionable guidance throughout
</verification>

<success_criteria>
- Client developers can implement full integration without additional documentation
- Common error scenarios are documented with solutions
- Security best practices are clearly explained
- Multiple language examples enable quick implementation
- Troubleshooting section addresses real integration issues
</success_criteria>

<output>
After completion, create `.planning/quick/260421-0u3-create-comprehensive-client-application-/260421-0u3-SUMMARY.md`
</output>
