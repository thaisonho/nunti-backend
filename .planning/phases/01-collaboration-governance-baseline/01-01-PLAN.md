---
phase: 01-collaboration-governance-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commitlint.config.mjs
  - .github/workflows/governance-pr-title.yml
  - .github/workflows/governance-commitlint.yml
  - .github/governance/rulesets/main-develop-governance.json
  - .github/scripts/apply-governance-ruleset.sh
autonomous: true
requirements:
  - GIT-02
  - GIT-03
must_haves:
  truths:
    - Pull requests to main and develop are blocked until one non-author approval exists.
    - New commits on an approved pull request invalidate stale approvals.
    - Conventional Commit policy is enforced for both pull request title and commit messages.
  artifacts:
    - path: commitlint.config.mjs
      provides: Conventional Commit type and breaking-change policy contract
    - path: .github/workflows/governance-pr-title.yml
      provides: Required status check for pull request title conformance
    - path: .github/workflows/governance-commitlint.yml
      provides: Required status check for commit message conformance
    - path: .github/governance/rulesets/main-develop-governance.json
      provides: Branch governance ruleset specification for main and develop
    - path: .github/scripts/apply-governance-ruleset.sh
      provides: Repeatable CLI application path for ruleset enforcement
  key_links:
    - from: .github/workflows/governance-pr-title.yml
      to: commitlint policy scope in phase context
      via: Matching allowed commit types in workflow action configuration
      pattern: feat|fix|docs|style|refactor|perf|test|build|ci|chore|revert
    - from: .github/workflows/governance-commitlint.yml
      to: commitlint.config.mjs
      via: configFile input
      pattern: commitlint.config.mjs
    - from: .github/governance/rulesets/main-develop-governance.json
      to: workflow job names
      via: required status checks list
      pattern: governance/pr-title|governance/commitlint
---

<objective>
Create enforceable repository-level governance controls for review gating and commit semantics.

Purpose: Turn collaboration policy into server-side controls that cannot be bypassed by local setup.
Output: Governance workflows, Conventional Commit rules, and ruleset-as-code artifact with apply script.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-collaboration-governance-baseline/01-CONTEXT.md
@.planning/phases/01-collaboration-governance-baseline/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Conventional Commit enforcement contract for pull request titles and commits</name>
  <files>commitlint.config.mjs, .github/workflows/governance-pr-title.yml, .github/workflows/governance-commitlint.yml</files>
  <behavior>
    - Invalid pull request title format fails governance/pr-title.
    - Commit types outside allowed list fail governance/commitlint.
    - Breaking-change notation is accepted through Conventional Commit grammar.
  </behavior>
  <action>Create commitlint.config.mjs extending @commitlint/config-conventional and restrict type-enum to feat, fix, docs, style, refactor, perf, test, build, ci, chore, revert. Add .github/workflows/governance-pr-title.yml using amannn/action-semantic-pull-request with the same type list and job name governance/pr-title. Add .github/workflows/governance-commitlint.yml using wagoid/commitlint-github-action with job name governance/commitlint and configFile commitlint.config.mjs. Keep check names globally unique and stable for branch ruleset binding.</action>
  <verify>
    <automated>rg -n "governance/pr-title|governance/commitlint|commitlint.config.mjs|action-semantic-pull-request|wagoid/commitlint-github-action" .github/workflows commitlint.config.mjs</automated>
  </verify>
  <done>Both governance workflows exist with stable job names and commit policy type list exactly matches locked decision set.</done>
</task>

<task type="auto">
  <name>Task 2: Define ruleset-as-code for protected integration branches</name>
  <files>.github/governance/rulesets/main-develop-governance.json</files>
  <action>Create a repository ruleset JSON specification targeting main and develop that requires pull requests, at least one approval, stale approval dismissal on new commits, required status checks governance/pr-title and governance/commitlint, conversation resolution, and blocked force-push/delete. Keep schema fields explicit so the file can be used with gh api without manual editing.</action>
  <verify>
    <automated>jq -e '.target=="branch" and (.conditions.ref_name.include | index("refs/heads/main")) != null and (.conditions.ref_name.include | index("refs/heads/develop")) != null and (.rules | length) &gt; 0' .github/governance/rulesets/main-develop-governance.json</automated>
  </verify>
  <done>Ruleset JSON fully encodes review gate and required-check policy for both integration branches.</done>
</task>

<task type="auto">
  <name>Task 3: Add CLI automation to apply governance ruleset</name>
  <files>.github/scripts/apply-governance-ruleset.sh</files>
  <action>Create an idempotent bash script that reads owner/repo from environment (or gh repo view fallback), then creates or updates the ruleset via gh api using .github/governance/rulesets/main-develop-governance.json. Include clear error messages for missing gh auth and non-admin permissions. Do not hardcode repository identity.</action>
  <verify>
    <automated>bash -n .github/scripts/apply-governance-ruleset.sh</automated>
  </verify>
  <done>Script validates syntactically and contains create/update flow using gh api with the phase ruleset JSON artifact.</done>
</task>

</tasks>

<verification>
Run static checks to confirm workflow names, ruleset fields, and automation script syntax:
- rg patterns for workflow linkage and job-name stability
- jq structural assertions over ruleset JSON
- bash -n for apply script syntax
</verification>

<success_criteria>
Repository contains machine-readable controls that enforce PR review gates and Conventional Commit conformance for integration branches.
</success_criteria>

<output>
After completion, create `.planning/phases/01-collaboration-governance-baseline/01-01-SUMMARY.md`
</output>
