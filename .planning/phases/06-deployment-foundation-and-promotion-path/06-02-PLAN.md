---
phase: 06-deployment-foundation-and-promotion-path
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - scripts/deploy/promote-release.sh
  - scripts/deploy/rollback-release.sh
  - .github/workflows/release-promote.yml
  - .github/workflows/release-rollback.yml
  - docs/deployment/release-promotion-runbook.md
autonomous: true
requirements:
  - DEP-02
must_haves:
  truths:
    - "Production promotion reuses the exact artifact already approved in staging."
    - "Team can trigger an explicit rollback to the prior known-good release artifact."
  artifacts:
    - path: .github/workflows/release-promote.yml
      provides: "Promotion workflow that forbids rebuild and consumes staging-approved manifest"
    - path: .github/workflows/release-rollback.yml
      provides: "Explicit rollback workflow for previous approved release"
    - path: docs/deployment/release-promotion-runbook.md
      provides: "Operational procedure for promote and rollback"
  key_links:
    - from: .github/workflows/release-promote.yml
      to: scripts/deploy/promote-release.sh
      via: "Manifest-driven production deploy"
      pattern: "releaseVersion|templateSha256|no rebuild"
    - from: .github/workflows/release-rollback.yml
      to: scripts/deploy/rollback-release.sh
      via: "Previous-approved release redeploy"
      pattern: "rollback|previous approved"
---

<objective>
Implement immutable promotion and explicit rollback path for production releases.

Purpose: DEP-02 requires artifact provenance between staging and production plus deterministic rollback to prior approved version.
Output: Production promote workflow, rollback workflow, and operator runbook wired to release manifest inputs.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-deployment-foundation-and-promotion-path/06-CONTEXT.md
@.planning/phases/06-deployment-foundation-and-promotion-path/06-RESEARCH.md
@.planning/phases/06-deployment-foundation-and-promotion-path/06-01-PLAN.md
@CONTRIBUTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add manifest-driven production promotion with provenance checks</name>
  <read_first>
    - .planning/phases/06-deployment-foundation-and-promotion-path/06-01-PLAN.md
    - deploy/release-manifest.schema.json
    - scripts/deploy/deploy-stage.sh
  </read_first>
  <files>scripts/deploy/promote-release.sh, .github/workflows/release-promote.yml</files>
  <action>
    Create `scripts/deploy/promote-release.sh` that accepts `releaseVersion` and manifest location, validates required manifest fields, verifies `templateSha256` is present, and deploys production using the packaged template/artifact pointer from the manifest.

    Ensure promote script explicitly forbids rebuild by not invoking `npm ci`, `npm run build`, or `sam build`.

    Create `.github/workflows/release-promote.yml` as a manual `workflow_dispatch` job requiring `releaseVersion`, pulls the staging-approved `release-manifest.json`, runs provenance checks (releaseVersion + templateSha256), and executes `scripts/deploy/promote-release.sh`.
  </action>
  <verify>
    <automated>rg "workflow_dispatch|releaseVersion|templateSha256|promote-release.sh|do not rebuild|no rebuild" .github/workflows/release-promote.yml scripts/deploy/promote-release.sh</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/release-promote.yml` uses `workflow_dispatch` with a `releaseVersion` input.
    - `scripts/deploy/promote-release.sh` checks for `releaseVersion` and `templateSha256` before deploy.
    - Promotion workflow references staging-approved `release-manifest.json` and does not run build/package commands.
    - Production deploy command source is the manifest artifact pointer, not local rebuild output.
  </acceptance_criteria>
  <done>
    Production promotion is immutable, traceable, and tied to the exact staging-approved artifact.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement explicit rollback workflow and operator runbook</name>
  <read_first>
    - .github/workflows/release-promote.yml
    - scripts/deploy/promote-release.sh
    - CONTRIBUTING.md
  </read_first>
  <files>scripts/deploy/rollback-release.sh, .github/workflows/release-rollback.yml, docs/deployment/release-promotion-runbook.md</files>
  <action>
    Create `scripts/deploy/rollback-release.sh` that takes `targetEnvironment` and `releaseVersion`, resolves the previous approved release manifest, validates required manifest fields, and redeploys that manifest artifact.

    Create `.github/workflows/release-rollback.yml` with manual dispatch inputs (`targetEnvironment`, `releaseVersion`) that runs rollback script and prints rollback metadata (releaseVersion, gitSha, templateSha256) in job summary.

    Create `docs/deployment/release-promotion-runbook.md` with concrete steps for staging deploy, production promote, and rollback, including the exact workflow names and required inputs.
  </action>
  <verify>
    <automated>rg "rollback|workflow_dispatch|targetEnvironment|releaseVersion|templateSha256|release-promotion-runbook" .github/workflows/release-rollback.yml scripts/deploy/rollback-release.sh docs/deployment/release-promotion-runbook.md</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/release-rollback.yml` exists with manual inputs `targetEnvironment` and `releaseVersion`.
    - `scripts/deploy/rollback-release.sh` redeploys a previous approved manifest artifact instead of rebuilding source.
    - Rollback workflow emits manifest provenance fields (`releaseVersion`, `gitSha`, `templateSha256`) in output/log summary.
    - `docs/deployment/release-promotion-runbook.md` includes numbered steps for deploy -> promote -> rollback with exact workflow file names.
  </acceptance_criteria>
  <done>
    Operators can execute an explicit, artifact-based rollback path from documented runbook instructions.
  </done>
</task>

</tasks>

<verification>
Run:
- `rg "workflow_dispatch|releaseVersion|templateSha256|rollback|targetEnvironment" .github/workflows/release-promote.yml .github/workflows/release-rollback.yml scripts/deploy/promote-release.sh scripts/deploy/rollback-release.sh`
- `npm run build`

Wave 2 passes when production promotion and rollback are both manifest-driven and rebuild-free.
</verification>

<success_criteria>
DEP-02 is satisfied by immutable artifact promotion to production and explicit rollback to previous approved release manifest.
</success_criteria>

<output>
After completion, create `.planning/phases/06-deployment-foundation-and-promotion-path/06-02-SUMMARY.md`
</output>
