---
phase: 05-groups-fanout-and-attachments
plan: 01
type: execute
wave: 1
depends_on:
  - "04-04"
files_modified:
  - src/messages/group-message-model.ts
  - src/messages/group-message-repository.ts
  - src/messages/group-message-service.ts
  - src/realtime/group-relay-publisher.ts
  - src/handlers/ws/group-membership.ts
  - src/handlers/ws/reconnect.ts
  - tests/unit/group-message-model.test.ts
  - tests/unit/group-message-repository.test.ts
  - tests/unit/group-message-service.test.ts
  - tests/unit/group-relay-publisher.test.ts
  - tests/integration/groups-membership-events.test.ts
  - tests/integration/groups-reconnect-replay.test.ts
autonomous: true
requirements:
  - GRP-01
must_haves:
  truths:
    - "Group members receive membership delta events (join, leave, remove-by-admin, role update, profile update), including the actor."
    - "Membership events contain stable eventId and replay in strict order on reconnect for missed events."
    - "Membership operation failures return structured websocket error payloads with code, message, and requestId."
  artifacts:
    - path: src/messages/group-message-model.ts
      provides: "Membership event and validation contracts including eventId and ordering anchors"
    - path: src/messages/group-message-repository.ts
      provides: "Persistence primitives for membership event timeline and per-device replay projections"
    - path: src/messages/group-message-service.ts
      provides: "Membership change orchestration and replay ordering logic"
    - path: src/realtime/group-relay-publisher.ts
      provides: "Membership event fanout publisher with stale-connection cleanup"
    - path: src/handlers/ws/group-membership.ts
      provides: "WebSocket membership route with structured error handling"
  key_links:
    - from: src/handlers/ws/group-membership.ts
      to: src/messages/group-message-service.ts
      via: "validated membership command handling"
      pattern: "changeType|requestId|error"
    - from: src/messages/group-message-service.ts
      to: src/messages/group-message-repository.ts
      via: "eventId allocation and ordered projection writes"
      pattern: "eventId|serverTimestamp|projection"
    - from: src/messages/group-message-service.ts
      to: src/realtime/group-relay-publisher.ts
      via: "fanout to all current group members including actor"
      pattern: "publishMembershipEvent|member-"
---

<objective>
Build the membership-event substrate for group messaging.

Purpose: Group send fanout and replay correctness depend on a deterministic membership-event timeline with stable identifiers and reconnect continuity.
Output: Typed membership contracts, ordered persistence/projection primitives, membership websocket handling, and replay coverage for missed membership events.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-groups-fanout-and-attachments/05-CONTEXT.md
@.planning/phases/05-groups-fanout-and-attachments/05-RESEARCH.md
@src/messages/message-model.ts
@src/messages/message-repository.ts
@src/messages/message-service.ts
@src/realtime/message-relay-publisher.ts
@src/handlers/ws/reconnect.ts
@tests/integration/messages-reconnect.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define membership contracts and ordered persistence projections</name>
  <read_first>
    - .planning/phases/05-groups-fanout-and-attachments/05-CONTEXT.md
    - src/messages/group-message-model.ts
    - src/messages/group-message-repository.ts
    - tests/unit/group-message-model.test.ts
    - tests/unit/group-message-repository.test.ts
  </read_first>
  <files>src/messages/group-message-model.ts, src/messages/group-message-repository.ts, tests/unit/group-message-model.test.ts, tests/unit/group-message-repository.test.ts</files>
  <behavior>
    - Membership event payloads use delta shape only and include changeType, actorUserId, targetUserId, serverTimestamp, and stable eventId.
    - Ordering is deterministic by serverTimestamp plus monotonic eventId, and projection keys preserve that replay order.
    - Invalid membership payloads are rejected through strict schema validation.
  </behavior>
  <action>
    Create group membership event contracts and zod validators with the locked change types: member-joined, member-left, member-removed-by-admin, member-role-updated, and group-profile-updated.

    Implement repository helpers for writing canonical membership events and per-device replay projections using deterministic ordering keys. Keep this additive to existing message repository patterns, and do not introduce full member-list snapshots in event payloads.
  </action>
  <verify>
    <automated>npm test -- tests/unit/group-message-model.test.ts tests/unit/group-message-repository.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/messages/group-message-model.ts` defines membership change types: `member-joined`, `member-left`, `member-removed-by-admin`, `member-role-updated`, `group-profile-updated`.
    - Membership event schema enforces `changeType`, `actorUserId`, `targetUserId`, `serverTimestamp`, and `eventId` as required fields.
    - `src/messages/group-message-repository.ts` includes deterministic ordering anchors using both `serverTimestamp` and monotonic `eventId` for replay projections.
    - `npm test -- tests/unit/group-message-model.test.ts tests/unit/group-message-repository.test.ts` exits 0.
  </acceptance_criteria>
  <done>
    Membership event shape and storage order are fixed and test-covered, providing a stable foundation for fanout and reconnect replay.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement membership fanout route and reconnect replay</name>
  <read_first>
    - .planning/phases/05-groups-fanout-and-attachments/05-CONTEXT.md
    - src/messages/group-message-service.ts
    - src/realtime/group-relay-publisher.ts
    - src/handlers/ws/group-membership.ts
    - src/handlers/ws/reconnect.ts
    - tests/integration/groups-membership-events.test.ts
    - tests/integration/groups-reconnect-replay.test.ts
  </read_first>
  <files>src/messages/group-message-service.ts, src/realtime/group-relay-publisher.ts, src/handlers/ws/group-membership.ts, src/handlers/ws/reconnect.ts, tests/unit/group-message-service.test.ts, tests/unit/group-relay-publisher.test.ts, tests/integration/groups-membership-events.test.ts, tests/integration/groups-reconnect-replay.test.ts</files>
  <behavior>
    - Accepted membership changes fan out to all current group members including actor.
    - Failed membership commands emit structured websocket error events with code, generic message, and requestId.
    - Devices that reconnect receive missed membership events in order and then a replay-complete boundary.
  </behavior>
  <action>
    Implement membership command orchestration and publisher wiring for live fanout, including stale connection cleanup parity with current publisher behavior.

    Extend reconnect handling to include membership replay for queued events, preserving oldest-first drain semantics and explicit replay-complete signaling. Keep event payloads delta-based and include both actorUserId and targetUserId for leave/remove flows.
  </action>
  <verify>
    <automated>npm test -- tests/unit/group-message-service.test.ts tests/unit/group-relay-publisher.test.ts tests/integration/groups-membership-events.test.ts tests/integration/groups-reconnect-replay.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/handlers/ws/group-membership.ts` emits structured websocket errors with `code`, `message`, and `requestId` for rejected membership commands.
    - `src/messages/group-message-service.ts` publishes accepted membership deltas to all current members including the actor.
    - `src/handlers/ws/reconnect.ts` replays missed membership events oldest-first and emits replay-complete boundary even when no events are replayed.
    - `npm test -- tests/unit/group-message-service.test.ts tests/unit/group-relay-publisher.test.ts tests/integration/groups-membership-events.test.ts tests/integration/groups-reconnect-replay.test.ts` exits 0.
  </acceptance_criteria>
  <done>
    Group membership event routing and reconnect replay are operational and deterministic for all locked membership change types.
  </done>
</task>

</tasks>

<verification>
Run membership slice first, then full suite:

- `npm test -- tests/unit/group-message-model.test.ts tests/unit/group-message-repository.test.ts tests/unit/group-message-service.test.ts tests/unit/group-relay-publisher.test.ts tests/integration/groups-membership-events.test.ts tests/integration/groups-reconnect-replay.test.ts`
- `npm test`

Wave 1 is complete when membership events route correctly, replay in order, and return structured errors on failures.
</verification>

<success_criteria>
Group membership lifecycle events are machine-consumable, delivered to relevant members including actor, and replay safely on reconnect with stable dedup/order anchors.
</success_criteria>

<output>
After completion, create `.planning/phases/05-groups-fanout-and-attachments/05-01-SUMMARY.md`.
</output>
