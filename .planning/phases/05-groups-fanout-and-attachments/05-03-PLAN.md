---
phase: 05-groups-fanout-and-attachments
plan: 03
type: execute
wave: 3
depends_on:
  - "05-02"
files_modified:
  - src/messages/group-message-model.ts
  - src/messages/group-message-service.ts
  - src/handlers/ws/group-messages.ts
  - src/realtime/group-relay-publisher.ts
  - tests/unit/group-message-model.test.ts
  - tests/unit/group-message-service.test.ts
  - tests/integration/groups-attachments.test.ts
  - tests/integration/groups-fanout.test.ts
autonomous: true
requirements:
  - GRP-04
must_haves:
  truths:
    - "Group send accepts encrypted attachment envelopes (metadata only), not binary media payloads."
    - "Missing or invalid required attachment metadata is rejected before fanout with structured error event fields."
    - "Attachment-envelope group messages use the same ordering and replay path as non-attachment group messages."
  artifacts:
    - path: src/messages/group-message-model.ts
      provides: "Attachment envelope schema with required/optional metadata and strict validation"
    - path: src/messages/group-message-service.ts
      provides: "Pre-fanout attachment validation and envelope propagation in canonical records and projection events"
    - path: src/handlers/ws/group-messages.ts
      provides: "Group-send request parsing and rejection path for invalid attachment envelope payloads"
    - path: tests/integration/groups-attachments.test.ts
      provides: "End-to-end envelope transport and validation rejection coverage"
  key_links:
    - from: src/handlers/ws/group-messages.ts
      to: src/messages/group-message-model.ts
      via: "strict schema parse of attachment envelopes"
      pattern: "attachmentId|storagePointer|mimeType|byteSize|contentHash"
    - from: src/messages/group-message-service.ts
      to: src/realtime/group-relay-publisher.ts
      via: "envelope metadata transport in same ordering/event path"
      pattern: "attachments|serverTimestamp|eventId"
    - from: tests/integration/groups-attachments.test.ts
      to: src/messages/group-message-service.ts
      via: "invalid envelope rejects before projections, valid envelope fans out"
      pattern: "VALIDATION_ERROR|group-message"
---

<objective>
Add encrypted attachment-envelope transport to group messaging.

Purpose: Phase 5 requires metadata-only attachment support that preserves E2EE boundaries and integrates with existing group fanout/replay ordering behavior.
Output: Strict attachment envelope validation, websocket rejection path, and transport of validated envelopes through group fanout and replay event contracts.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-groups-fanout-and-attachments/05-CONTEXT.md
@.planning/phases/05-groups-fanout-and-attachments/05-RESEARCH.md
@.planning/phases/05-groups-fanout-and-attachments/05-02-SUMMARY.md
@src/messages/group-message-model.ts
@src/messages/group-message-service.ts
@src/handlers/ws/group-messages.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add strict attachment-envelope schema and rejection behavior</name>
  <read_first>
    - .planning/phases/05-groups-fanout-and-attachments/05-CONTEXT.md
    - .planning/phases/05-groups-fanout-and-attachments/05-02-SUMMARY.md
    - src/messages/group-message-model.ts
    - src/handlers/ws/group-messages.ts
    - tests/integration/groups-attachments.test.ts
  </read_first>
  <files>src/messages/group-message-model.ts, src/handlers/ws/group-messages.ts, tests/unit/group-message-model.test.ts, tests/integration/groups-attachments.test.ts</files>
  <behavior>
    - Required attachment fields are attachmentId, storagePointer, mimeType, byteSize, and contentHash.
    - Optional fields originalFileName and thumbnailPointer are accepted when present.
    - Binary media payload fields are rejected, and invalid envelope metadata returns structured validation error with code, message, and requestId.
  </behavior>
  <action>
    Extend group-message validation schema with strict envelope-only attachment metadata constraints, including positive integer byteSize and empty-string rejection for required fields.

    Wire group websocket handler to reject invalid attachment input before canonical write or any fanout side effects. Keep error contract machine-readable and generic per locked decisions.
  </action>
  <verify>
    <automated>npm test -- tests/unit/group-message-model.test.ts tests/integration/groups-attachments.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/messages/group-message-model.ts` requires attachment fields `attachmentId`, `storagePointer`, `mimeType`, `byteSize`, and `contentHash`, with optional `originalFileName` and `thumbnailPointer`.
    - Attachment schema enforces MIME allowlist, `byteSize <= 26214400`, `contentHash` SHA-256 format, and maximum 10 envelopes per message.
    - `src/handlers/ws/group-messages.ts` rejects invalid attachment payloads before canonical writes/fanout and emits structured validation errors with `code`, `message`, and `requestId`.
    - `npm test -- tests/unit/group-message-model.test.ts tests/integration/groups-attachments.test.ts` exits 0.
  </acceptance_criteria>
  <done>
    Invalid attachment envelopes fail early with structured errors, and valid metadata-only envelopes pass schema validation.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Transport validated attachment envelopes through fanout and replay</name>
  <read_first>
    - .planning/phases/05-groups-fanout-and-attachments/05-CONTEXT.md
    - src/messages/group-message-service.ts
    - src/realtime/group-relay-publisher.ts
    - src/handlers/ws/reconnect.ts
    - tests/integration/groups-attachments.test.ts
    - tests/integration/groups-fanout.test.ts
  </read_first>
  <files>src/messages/group-message-service.ts, src/realtime/group-relay-publisher.ts, tests/unit/group-message-service.test.ts, tests/integration/groups-attachments.test.ts, tests/integration/groups-fanout.test.ts</files>
  <behavior>
    - Valid attachment envelope metadata is persisted and delivered with normal group-message ordering/events.
    - Offline queued projections preserve attachment metadata for replay on reconnect.
    - Per-device status behavior remains unchanged and still reports delivered/accepted-queued/failed.
  </behavior>
  <action>
    Update group service and publisher event payload contracts so attachment envelope metadata travels through canonical storage, live fanout, and replay without introducing a separate transport channel.

    Preserve existing ordering anchors and per-device outcome semantics from plan 05-02. Do not implement media upload/download or plaintext payload handling in this phase.
  </action>
  <verify>
    <automated>npm test -- tests/unit/group-message-service.test.ts tests/integration/groups-attachments.test.ts tests/integration/groups-fanout.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/messages/group-message-service.ts` persists validated attachment envelope metadata in canonical records and device projections.
    - `src/realtime/group-relay-publisher.ts` includes attachment envelope metadata in live and replay group-message payloads without introducing binary transport fields.
    - Per-device status behavior remains `delivered|accepted-queued|failed` for attachment-bearing messages.
    - `npm test -- tests/unit/group-message-service.test.ts tests/integration/groups-attachments.test.ts tests/integration/groups-fanout.test.ts` exits 0.
  </acceptance_criteria>
  <done>
    Attachment envelopes are fully supported through group send/fanout/replay while preserving E2EE envelope-only boundaries.
  </done>
</task>

</tasks>

<verification>
Run attachment-focused slice first, then full suite:

- `npm test -- tests/unit/group-message-model.test.ts tests/unit/group-message-service.test.ts tests/integration/groups-attachments.test.ts tests/integration/groups-fanout.test.ts`
- `npm test`

Wave 3 is complete when attachment envelopes validate strictly, invalid payloads reject pre-fanout, and valid envelopes flow through live and replay paths.
</verification>

<success_criteria>
Users can send and receive encrypted attachment envelopes as metadata-only payloads through the existing group message transport, with strict validation and replay parity.
</success_criteria>

<output>
After completion, create `.planning/phases/05-groups-fanout-and-attachments/05-03-SUMMARY.md`.
</output>
