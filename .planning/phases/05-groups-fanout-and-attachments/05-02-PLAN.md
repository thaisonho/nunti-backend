---
phase: 05-groups-fanout-and-attachments
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - src/messages/group-message-model.ts
  - src/messages/group-message-repository.ts
  - src/messages/group-message-service.ts
  - src/realtime/group-relay-publisher.ts
  - src/handlers/ws/group-messages.ts
  - src/handlers/ws/reconnect.ts
  - tests/unit/group-message-repository.test.ts
  - tests/unit/group-message-service.test.ts
  - tests/unit/group-relay-publisher.test.ts
  - tests/integration/groups-fanout.test.ts
  - tests/integration/groups-idempotency.test.ts
  - tests/integration/groups-reconnect-replay.test.ts
autonomous: true
requirements:
  - GRP-02
  - GRP-03
must_haves:
  truths:
    - "Group message send accepts encrypted payload and returns immediate sender result with groupMessageId."
    - "Recipient audience is deterministic from accept-time membership snapshot and excludes sender user."
    - "Per-recipient-device outcomes are emitted asynchronously as delivered, accepted-queued, or failed."
    - "Recipient and sender-side mirror fanout reaches all trusted active devices with offline queue-and-replay continuity."
  artifacts:
    - path: src/messages/group-message-service.ts
      provides: "Deterministic accept-time membership snapshot, idempotent group send, and per-device fanout orchestration"
    - path: src/messages/group-message-repository.ts
      provides: "Canonical group message writes, projection rows, and replay query/update primitives"
    - path: src/realtime/group-relay-publisher.ts
      provides: "Group message and per-device status event publishing"
    - path: src/handlers/ws/group-messages.ts
      provides: "WebSocket group send route and immediate accepted send-result contract"
  key_links:
    - from: src/handlers/ws/group-messages.ts
      to: src/messages/group-message-service.ts
      via: "validated group-send request"
      pattern: "groupMessageId|groupId|ciphertext"
    - from: src/messages/group-message-service.ts
      to: src/messages/group-message-repository.ts
      via: "idempotent canonical write and per-device projection persistence"
      pattern: "ConditionExpression|groupMessageId|device"
    - from: src/messages/group-message-service.ts
      to: src/realtime/group-relay-publisher.ts
      via: "per-device live publish and sender mirror/device status events"
      pattern: "delivered|accepted-queued|failed"
---

<objective>
Implement encrypted group message routing and multi-device fanout behavior.

Purpose: Phase 5 requires deterministic group audience selection plus per-device outcome reporting across both recipient devices and sender mirror devices.
Output: Group send route, idempotent group send orchestration, trusted-device fanout/queueing, and replay-compatible outcome publication.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-groups-fanout-and-attachments/05-CONTEXT.md
@.planning/phases/05-groups-fanout-and-attachments/05-RESEARCH.md
@.planning/phases/05-groups-fanout-and-attachments/05-01-SUMMARY.md
@src/devices/device-repository.ts
@src/messages/message-service.ts
@src/realtime/connection-registry.ts
@src/handlers/ws/messages.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement deterministic group send and idempotent projection writes</name>
  <read_first>
    - .planning/phases/05-groups-fanout-and-attachments/05-CONTEXT.md
    - .planning/phases/05-groups-fanout-and-attachments/05-01-SUMMARY.md
    - src/messages/group-message-model.ts
    - src/messages/group-message-repository.ts
    - src/messages/group-message-service.ts
    - tests/integration/groups-idempotency.test.ts
  </read_first>
  <files>src/messages/group-message-model.ts, src/messages/group-message-repository.ts, src/messages/group-message-service.ts, tests/unit/group-message-repository.test.ts, tests/unit/group-message-service.test.ts, tests/integration/groups-idempotency.test.ts</files>
  <behavior>
    - Group send resolves recipients from membership snapshot at server accept time and excludes sender from recipient audience.
    - Duplicate sends by same groupMessageId return prior stored result without duplicating projections or republishing side effects.
    - Per-device projection rows preserve ordering anchor for replay continuity.
  </behavior>
  <action>
    Extend group service/repository to persist canonical group messages and target-device projections atomically, keyed by groupMessageId as immutable idempotency key.

    Persist recipient snapshot (or equivalent deterministic recipient set) on accept so retries and async fanout cannot drift from the original audience. Keep partial success semantics by design and avoid all-or-nothing fanout enforcement.
  </action>
  <verify>
    <automated>npm test -- tests/unit/group-message-repository.test.ts tests/unit/group-message-service.test.ts tests/integration/groups-idempotency.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/messages/group-message-service.ts` computes recipient audience from accept-time membership snapshot and excludes sender from recipient audience.
    - `src/messages/group-message-repository.ts` stores canonical send records keyed by immutable `groupMessageId` and suppresses duplicate projection side effects on retries.
    - Projection rows include deterministic ordering anchors to preserve reconnect replay continuity.
    - `npm test -- tests/unit/group-message-repository.test.ts tests/unit/group-message-service.test.ts tests/integration/groups-idempotency.test.ts` exits 0.
  </acceptance_criteria>
  <done>
    Group send is deterministic and idempotent, and side effects are not duplicated under retries.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire websocket group-send handler, live fanout, sender mirror, and replay outcomes</name>
  <read_first>
    - .planning/phases/05-groups-fanout-and-attachments/05-CONTEXT.md
    - src/handlers/ws/group-messages.ts
    - src/realtime/group-relay-publisher.ts
    - src/handlers/ws/reconnect.ts
    - src/realtime/connection-registry.ts
    - tests/integration/groups-fanout.test.ts
    - tests/integration/groups-reconnect-replay.test.ts
  </read_first>
  <files>src/messages/group-message-service.ts, src/realtime/group-relay-publisher.ts, src/handlers/ws/group-messages.ts, src/handlers/ws/reconnect.ts, tests/unit/group-message-service.test.ts, tests/unit/group-relay-publisher.test.ts, tests/integration/groups-fanout.test.ts, tests/integration/groups-reconnect-replay.test.ts</files>
  <behavior>
    - Sender receives immediate accepted send-result containing groupMessageId.
    - Online recipient devices receive live group-message events; offline trusted devices are queued.
    - Sender's other trusted devices receive mirror copy events, and all per-device outcomes emit delivered, accepted-queued, or failed.
    - Reconnect drains queued group-message projections and preserves ordering behavior.
  </behavior>
  <action>
    Add websocket group send handler and service wiring for live publish plus queue fallback, targeting trusted devices from existing device policy/repository primitives.

    Emit asynchronous per-recipient-device and sender-mirror statuses, and integrate reconnect drain so queued group-message rows replay through the same ordering contract used for live projections.
  </action>
  <verify>
    <automated>npm test -- tests/unit/group-message-service.test.ts tests/unit/group-relay-publisher.test.ts tests/integration/groups-fanout.test.ts tests/integration/groups-reconnect-replay.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/handlers/ws/group-messages.ts` returns immediate accepted sender result containing `groupMessageId`, `recipientUserCount`, and `targetDeviceCount`.
    - `src/realtime/group-relay-publisher.ts` emits asynchronous per-device status events limited to `delivered`, `accepted-queued`, and `failed`.
    - Sender mirror fanout targets sender trusted devices with status events marked for `audience = sender-sync`.
    - `src/handlers/ws/reconnect.ts` replays queued group message projections through the same ordering path as live events.
    - `npm test -- tests/unit/group-message-service.test.ts tests/unit/group-relay-publisher.test.ts tests/integration/groups-fanout.test.ts tests/integration/groups-reconnect-replay.test.ts` exits 0.
  </acceptance_criteria>
  <done>
    Encrypted group send and multi-device fanout are operational across live, queued, and replay paths with per-device outcomes.
  </done>
</task>

</tasks>

<verification>
Run fanout and idempotency slices first, then full suite:

- `npm test -- tests/unit/group-message-repository.test.ts tests/unit/group-message-service.test.ts tests/unit/group-relay-publisher.test.ts tests/integration/groups-idempotency.test.ts tests/integration/groups-fanout.test.ts tests/integration/groups-reconnect-replay.test.ts`
- `npm test`

Wave 2 is complete when group send accepts deterministically, fanout covers trusted devices, and per-device statuses are stable across retries/reconnect.
</verification>

<success_criteria>
Users can send/receive encrypted group messages and backend fanout correctly covers recipient and sender trusted devices with deterministic outcomes and replay continuity.
</success_criteria>

<output>
After completion, create `.planning/phases/05-groups-fanout-and-attachments/05-02-SUMMARY.md`.
</output>
