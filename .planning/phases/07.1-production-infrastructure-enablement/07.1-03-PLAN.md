---
phase: 07.1-production-infrastructure-enablement
plan: 03
type: execute
wave: 2
depends_on: []
files_modified: [template.yaml, deploy/params.production.json, docs/deployment/cognito-production-setup.md]
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Production Cognito User Pool exists and is separate from staging"
    - "Production Cognito User Pool enforces stricter password policies than staging"
    - "Production Cognito App Client is separate and configured for production environment"
    - "Production Cognito parameters are documented for operators"
  artifacts:
    - path: "template.yaml"
      provides: "Cognito User Pool and App Client CloudFormation resources"
      contains: ["AWS::Cognito::UserPool", "AWS::Cognito::UserPoolClient"]
    - path: "deploy/params.production.json"
      provides: "Production Cognito User Pool and App Client IDs"
      contains: ["CognitoUserPoolId", "CognitoAppClientId"]
    - path: "docs/deployment/cognito-production-setup.md"
      provides: "Operator documentation for Cognito configuration and policy requirements"
      contains: ["Password Policy", "MFA Requirements", "Production Setup"]
  key_links:
    - from: "template.yaml"
      to: "Lambda environment variables (Globals section)"
      via: "COGNITO_USER_POOL_ID and COGNITO_APP_CLIENT_ID passed to runtime"
      pattern: "COGNITO_USER_POOL_ID.*!Ref.*Cognito"
    - from: "Cognito User Pool policies"
      to: "Protected routes (WebSocket, API endpoints)"
      via: "JWT validation in auth-guard.ts uses pool issuer and claims"
      pattern: "oid.*claim.*validation"
---

<objective>
Establish separate production Cognito User Pool with stricter identity policies and production App Client.

Purpose: Production Cognito must be completely isolated from staging with stricter password/MFA enforcement preventing weak authentication from compromising production identity layer.

Output: New Cognito User Pool resource in SAM template, production-specific identity configuration, operator setup documentation.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.planning/phases/07.1-production-infrastructure-enablement/07.1-CONTEXT.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-security-hardening-for-live-runtime/07-CONTEXT.md (Cognito and Identity section)
@.planning/phases/07.1-production-infrastructure-enablement/07.1-CONTEXT.md (Cognito Production Setup)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Cognito User Pool and App Client resources in template.yaml with production policies</name>
  <files>template.yaml</files>
  <read_first>
    - template.yaml (Globals section to understand environment variable injection)
    - src/auth/cognito-client.ts (to understand how pool ID and app client ID are used)
    - .planning/phases/07-security-hardening-for-live-runtime/07-CONTEXT.md (policy requirements)
  </read_first>
  <action>
Add to template.yaml Parameters section:
- ProductionCognitoEmailAttribute (String, Default: email, determines if email is required for user pool)
- CognitoPasswordMinLength (Number, Default: 12, for production use 12+ minimum)
- RequireCognitoMFA (String, AllowedValues: "true", "false", Default: "true" for production)

Add to template.yaml Resources section after DynamoDB definitions:

```yaml
  ProductionCognitoUserPool:
    Type: AWS::Cognito::UserPool
    Condition: IsProduction
    Properties:
      UserPoolName: nunti-production-user-pool
      Schema:
        - Name: email
          AttributeDataType: String
          Required: true
          Mutable: false
        - Name: phone_number
          AttributeDataType: String
          Mutable: true
        - Name: name
          AttributeDataType: String
          Mutable: true
      Policies:
        PasswordPolicy:
          MinimumLength: !Ref CognitoPasswordMinLength
          RequireUppercase: true
          RequireLowercase: true
          RequireNumbers: true
          RequireSymbols: true
          TemporaryPasswordValidityDays: 1
      MFAConfiguration: !If [RequireMFA, OPTIONAL, OFF]
      EnabledMfas:
        - SOFTWARE_TOKEN_MFA
        - SMS_MFA
      AuthPolicies:
        ComplexityConfiguration:
          RequireNumbers: true
          RequireSymbols: true
          RequireUppercase: true
          RequireLowercase: true
      AccountRecoverySetting:
        RecoveryMechanisms:
          - Name: verified_email
            Priority: 1
          - Name: verified_phone_number
            Priority: 2
      UserAttributeUpdateSettings:
        AttributesRequireVerificationBeforeBeing updated:
          - email
          - phone_number
      DeviceConfiguration:
        ChallengeRequiredOnNewDevice: true
        DeviceOnlyRememberedOnUserPrompt: false
      Tags:
        - Key: Environment
          Value: production
        - Key: SecurityLevel
          Value: strict

  ProductionCognitoUserPoolClient:
    Type: AWS::Cognito::UserPoolClient
    Condition: IsProduction
    Properties:
      ClientName: nunti-production-app
      UserPoolId: !Ref ProductionCognitoUserPool
      GenerateSecret: true
      AllowedOAuthFlows:
        - authorization_code
      AllowedOAuthScopes:
        - email
        - openid
      AllowedOAuthFlowsUserPoolClient: true
      ExplicitAuthFlows:
        - ADMIN_NO_SRP_AUTH
        - USER_PASSWORD_AUTH
      PreventUserExistenceErrors: LEGACY
      RefreshTokenValidity: 30
      AccessTokenValidity: 1
      IdTokenValidity: 1
      TokenValidityUnits:
        RefreshToken: days
        AccessToken: hours
        IdToken: hours
      SupportedIdentityProviders:
        - COGNITO
      AllowUserPasswordAuthFlow: true
      EnableTokenRevocation: true
      EnablePropagateAttributes: true
```

Add to Conditions section:
```yaml
  RequireMFA: !Equals [!Ref RequireCognitoMFA, "true"]
```

Add to Outputs section:
```yaml
  ProductionCognitoUserPoolId:
    Condition: IsProduction
    Value: !Ref ProductionCognitoUserPool
    Export:
      Name: !Sub "nunti-cognito-user-pool-id-${Stage}"
  ProductionCognitoAppClientId:
    Condition: IsProduction
    Value: !Ref ProductionCognitoUserPoolClient
    Export:
      Name: !Sub "nunti-cognito-app-client-id-${Stage}"
  ProductionCognitoIssuer:
    Condition: IsProduction
    Value: !Sub "https://cognito-idp.${AWS::Region}.amazonaws.com/${ProductionCognitoUserPool}"
    Export:
      Name: !Sub "nunti-cognito-issuer-${Stage}"
```

Key decisions:
- MFA is optional initially but can be required by setting RequireCognitoMFA=true in params
- Password minimum length of 12 characters (industry standard for production)
- Token expiry: 1-hour access tokens, 30-day refresh tokens (balance security vs UX)
- Phone number and email require verification before update (prevents account takeover)
- Device recognition enabled (device challenges on new sign-in)
  </action>
  <verify>
    <automated>grep -q "AWS::Cognito::UserPool" template.yaml && grep -q "ProductionCognitoUserPool" template.yaml && grep -q "MinimumLength" template.yaml && grep -q "RequireUppercase: true" template.yaml</automated>
  </verify>
  <done>template.yaml contains production Cognito User Pool with strict password policies (12+ char, uppercase, lowercase, numbers, symbols), device recognition, optional MFA, and proper token expiry. Cognito App Client configured with 1-hour access token and 30-day refresh token. Outputs export pool ID and issuer URL for lambda environment.</done>
</task>

<task type="auto">
  <name>Task 2: Update deploy/params.production.json with production Cognito User Pool and App Client IDs</name>
  <files>deploy/params.production.json</files>
  <read_first>
    - deploy/params.production.json (existing parameters)
    - template.yaml (parameter names from Resources section)
  </read_first>
  <action>
Update deploy/params.production.json parameters section to include:

Instead of placeholder values, replace with CloudFormation references that operators will populate:

```json
  "CognitoPasswordMinLength=12",
  "RequireCognitoMFA=true",
  "CognitoUserPoolId={output-from-first-deployment}",
  "CognitoAppClientId={output-from-first-deployment}"
```

Add comment above Cognito parameters:
```
// Production Cognito Configuration
// - CognitoPasswordMinLength: 12 characters minimum (enforced by User Pool policy)
// - RequireCognitoMFA: true = MFA required for all users in production
// - CognitoUserPoolId and CognitoAppClientId are populated from CloudFormation Outputs
//   after first deployment. See docs/deployment/cognito-production-setup.md
```

Note: After first SAM deployment creates the Cognito User Pool, operators must:
1. Extract CognitoUserPoolId from `sam deploy` output (shows as CloudFormation output)
2. Extract CognitoAppClientId from AWS Cognito Console
3. Update these values in subsequent deployments OR store as GitHub Actions secrets

Document this in the task action for clarity.
  </action>
  <verify>
    <automated>grep -q "CognitoPasswordMinLength=12" deploy/params.production.json && grep -q "RequireCognitoMFA=true" deploy/params.production.json</automated>
  </verify>
  <done>deploy/params.production.json includes production Cognito parameters with strict password requirements (12 chars) and MFA enabled by default. Comments document that pool IDs are obtained from CloudFormation Outputs after first deployment.</done>
</task>

<task type="auto">
  <name>Task 3: Create cognito-production-setup.md documentation for operators</name>
  <files>docs/deployment/cognito-production-setup.md</files>
  <read_first>
    - docs/deployment/environment-configs.md (reference format for operator docs)
    - .planning/phases/07-security-hardening-for-live-runtime/07-CONTEXT.md (policy requirements)
  </read_first>
  <action>
Create docs/deployment/cognito-production-setup.md:

```markdown
# Production Cognito Setup Guide

## Overview

Production Cognito User Pool is completely separate from staging and enforces stricter authentication policies:

| Policy | Staging | Production | Rationale |
|--------|---------|------------|-----------|
| Password Min Length | 8 | 12 | Industry standard for production |
| Uppercase Required | No | Yes | Increases entropy |
| Lowercase Required | No | Yes | Increases entropy |
| Numbers Required | No | Yes | Increases entropy |
| Symbols Required | No | Yes | Increases entropy |
| MFA | Optional | Required | Prevents account takeover |
| Device Recognition | No | Yes | Detects unauthorized sign-ins |
| Token Expiry (Access) | 24h | 1h | Reduces leaked token window |
| Token Expiry (Refresh) | 90d | 30d | Shorter session lifetime |

## Initial Setup

### 1. Deploy Production Stack with Cognito

First deployment of template.yaml will create a new Cognito User Pool:

\`\`\`bash
./scripts/deploy/deploy-stage.sh production
\`\`\`

This will:
- Create `nunti-production-user-pool` User Pool
- Create `nunti-production-app` App Client
- Output User Pool ID and App Client ID as CloudFormation exports

### 2. Capture Cognito IDs

After successful SAM deployment, retrieve the IDs:

\`\`\`bash
aws cloudformation describe-stacks \
  --stack-name nunti-backend-production \
  --query 'Stacks[0].Outputs[?OutputKey==\`ProductionCognitoUserPoolId\`].OutputValue' \
  --output text
\`\`\`

Output will be: `us-east-1_XXXXXXXXX`

Save this value as `PROD_COGNITO_USER_POOL_ID` in GitHub Actions secrets for future deployments.

### 3. Store in GitHub Actions Secrets

Update your GitHub repository secrets:
- `PROD_COGNITO_USER_POOL_ID` = `us-east-1_XXXXXXXXX`
- `PROD_COGNITO_APP_CLIENT_ID` = `abcd1234efgh5678ijkl9012` (find in AWS Cognito Console)

These are used in `.github/workflows/release-deploy.yml` to populate params.production.json.

## User Creation and Testing

### Create Test User

\`\`\`bash
aws cognito-idp admin-create-user \
  --user-pool-id us-east-1_XXXXXXXXX \
  --username testuser@example.com \
  --message-action SUPPRESS \
  --temporary-password TempPassword123!
\`\`\`

### Set Permanent Password

\`\`\`bash
aws cognito-idp admin-set-user-password \
  --user-pool-id us-east-1_XXXXXXXXX \
  --username testuser@example.com \
  --password ProductionPassword123! \
  --permanent
\`\`\`

### Enable MFA (if required)

\`\`\`bash
aws cognito-idp admin-set-user-mfa-preference \
  --user-pool-id us-east-1_XXXXXXXXX \
  --username testuser@example.com \
  --software-token-mfa-settings Enabled true SetAsPreferred true
\`\`\`

## Password Policy Enforcement

Production Cognito requires:
- **Minimum 12 characters** (no exceptions)
- **At least one uppercase letter** (A-Z)
- **At least one lowercase letter** (a-z)
- **At least one number** (0-9)
- **At least one special character** (!@#$%^&*)

Example valid password: \`MyApp@2024Prod!\`

Example invalid password: \`production123\` (no uppercase, symbols)

## MFA Setup

### Software Token MFA (TOTP)

Users authenticate with Time-based One-Time Password (TOTP) apps:
- Google Authenticator
- Microsoft Authenticator
- Authy

When disabled, users can still use SMS MFA if registered.

### SMS MFA

Requires user to have verified phone number in User Pool:

\`\`\`bash
aws cognito-idp admin-update-user-attributes \
  --user-pool-id us-east-1_XXXXXXXXX \
  --username testuser@example.com \
  --user-attributes Name=phone_number,Value=+12025551234
\`\`\`

## Device Configuration

Production Cognito tracks devices and requires challenges:
- First sign-in from new device → device challenge
- Existing device → seamless sign-in
- Device recognition prevents account takeover from new locations

Users can manage trusted devices in app settings (Phase 12+).

## Account Recovery

If user forgets password:

1. **Verified Email** (Primary recovery method)
   - User clicks "Forgot Password" → receives verification code to email
   - Enters code + new password in auth flow
   
2. **Verified Phone Number** (Secondary)
   - If email not verified, SMS sent to phone
   - User enters SMS code to reset password

## Monitoring and Troubleshooting

### Check User Pool Health

\`\`\`bash
aws cognito-idp describe-user-pool \
  --user-pool-id us-east-1_XXXXXXXXX \
  --query 'UserPool.[Id, Name, Status, UserAttributeUpdateSettings]'
\`\`\`

### List All Users

\`\`\`bash
aws cognito-idp list-users \
  --user-pool-id us-east-1_XXXXXXXXX
\`\`\`

### Check User Attributes

\`\`\`bash
aws cognito-idp admin-get-user \
  --user-pool-id us-east-1_XXXXXXXXX \
  --username testuser@example.com
\`\`\`

### Reset MFA (Emergency)

If user loses access to MFA device:

\`\`\`bash
aws cognito-idp admin-delete-user-attributes \
  --user-pool-id us-east-1_XXXXXXXXX \
  --username testuser@example.com \
  --user-attribute-names 'software_2fa_enabled'
\`\`\`

User will need to set up MFA again on next login.

## Common Issues

| Issue | Cause | Solution |
|-------|-------|----------|
| "Password does not conform to policy" | Missing uppercase, symbols, or number | Ensure password has all 4 types: uppercase, lowercase, number, symbol (min 12 chars) |
| "User account is locked" | Too many failed sign-in attempts | Wait 15 minutes or contact operator to unlock |
| "MFA challenge failed" | TOTP code expired (only valid 30 seconds) | Re-enter new code or use backup codes |
| "Device not recognized" | New device signing in for first time | Complete device challenge (code sent to email/phone) |

## Disaster Recovery

If Cognito User Pool is compromised or accidentally deleted:

1. **Emergency:** Disable Lambda functions (set concurrency to 0)
2. **Restore:** Create new pool from template or restore snapshot (if available)
3. **Migrate:** For user data, export user attributes via `admin-list-user-auth-events`
4. **Re-deploy:** Update GitHub Actions secret with new pool ID and redeploy

Note: User passwords are NOT recoverable from backups; users must reset via email verification.

## Phase 11 Enhancements

Operations Readiness (Phase 11) will add:
- Automated user provisioning via SCIM
- Custom auth flows for device-specific verification
- Integration with external IdP providers (OIDC, SAML)
- User audit logging to CloudWatch
- Cognito event webhooks for custom workflows
```
  </action>
  <verify>
    <automated>test -f docs/deployment/cognito-production-setup.md && grep -q "Password Policy Enforcement" docs/deployment/cognito-production-setup.md && grep -q "MFA Setup" docs/deployment/cognito-production-setup.md && grep -q "12 characters" docs/deployment/cognito-production-setup.md</automated>
  </verify>
  <done>Created comprehensive cognito-production-setup.md with policy table (staging vs production comparisons), user creation procedures, MFA setup, device tracking, account recovery flows, troubleshooting guide, and disaster recovery steps. Documentation is operator-ready.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User authentication → Cognito User Pool | Untrusted user credentials; must be validated against strict policy |
| Cognito → Lambda (JWT token) | Cognito-signed JWT passed to Lambda; Lambda validates signature and claims |
| Cognito → External IdP (future) | Future integrations with external identity providers (Phase 11+) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.1-15 | Spoofing | User identity | mitigate | "Cognito enforces password policy (12+ char, complexity). MFA required prevents account takeover even if password compromised. Device recognition flags sign-ins from new locations." |
| T-07.1-16 | Tampering | JWT token | mitigate | "Tokens are cryptographically signed by Cognito. Lambda validates signature (in auth-guard.ts) before accepting claims. Token expiry limits lifetime of leaked tokens to 1 hour." |
| T-07.1-17 | Repudiation | User actions | mitigate | "Cognito audit logs (CloudTrail) track sign-in attempts, password changes, MFA setup. Enable CloudTrail logging for compliance (Phase 11)." |
| T-07.1-18 | Information Disclosure | Password reset tokens | mitigate | "Password reset codes are one-time, expire in 24 hours (TemporaryPasswordValidityDays=1), sent via verified email only. Tokens are not logged or cached." |
| T-07.1-19 | Information Disclosure | MFA secret seeds | mitigate | "TOTP seeds are generated by Cognito, displayed once during setup, not retrievable after. Users must save backup codes during MFA enrollment (Phase 12+)." |
| T-07.1-20 | Denial of Service | User account lockout | accept | "After multiple failed auth attempts, account is temporarily locked (15 min). Accepted as security trade-off; Phase 11 can add unlock notifications." |

</threat_model>

<verification>
After task completion:
1. Validate template.yaml syntax: `sam validate template.yaml`
2. Verify params.production.json JSON: `jq . deploy/params.production.json`
3. Grep for Cognito resource definitions
4. Verify output exports reference IDs correctly
</verification>

<success_criteria>
- [x] template.yaml contains AWS::Cognito::UserPool with strict password policies (12+ char, all complexity types)
- [x] MFA is configurable (optional via parameter, but true by default in production)
- [x] Token expiry is set to 1h access / 30d refresh (appropriate for production)
- [x] Device recognition and email/phone verification enabled
- [x] deploy/params.production.json includes CognitoPasswordMinLength=12 and RequireCognitoMFA=true
- [x] Cognito User Pool and App Client IDs are exported as CloudFormation outputs
- [x] cognito-production-setup.md provides complete operator guide with policy table, setup steps, MFA procedures, troubleshooting, and disaster recovery
- [x] User Pool is completely separate from staging (no shared resources)
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-production-infrastructure-enablement/07.1-03-SUMMARY.md`
</output>
