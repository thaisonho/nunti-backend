---
phase: 07.1-production-infrastructure-enablement
plan: 04
type: execute
wave: 2
depends_on: []
files_modified: [template.yaml, deploy/params.production.json, docs/deployment/dns-and-cdn-setup.md]
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Route53 health check validates production API Gateway availability"
    - "Route53 DNS record routes to CloudFront distribution (not direct API Gateway)"
    - "CloudFront distribution is configured with origin to API Gateway, cache settings configured"
    - "CloudFront includes basic WAF for DDoS protection (AWS Shield Standard)"
    - "DNS and CDN configuration is documented for operators"
  artifacts:
    - path: "template.yaml"
      provides: "Route53 RecordSet and Health Check, CloudFront Distribution resources"
      contains: ["AWS::Route53::RecordSet", "AWS::Route53::HealthCheck", "AWS::CloudFront::Distribution"]
    - path: "deploy/params.production.json"
      provides: "Production Route53 zone ID and CloudFront domain configuration"
      contains: ["Route53HostedZoneId", "CloudFrontDomainName"]
    - path: "docs/deployment/dns-and-cdn-setup.md"
      provides: "Operator documentation for DNS records, health checks, and CloudFront configuration"
      contains: ["Route53 Setup", "CloudFront Configuration", "Health Check"]
  key_links:
    - from: "template.yaml CloudFront distribution"
      to: "API Gateway from Phase 6"
      via: "CloudFront origin points to API Gateway endpoint"
      pattern: "Origin.*ApiGateway|DomainName.*execute-api"
    - from: "Route53 health check"
      to: "API Gateway endpoint"
      via: "Health check monitors API Gateway response"
      pattern: "HealthCheckConfig.*ApiGateway"
    - from: "DNS record"
      to: "CloudFront distribution"
      via: "Route53 record points to CloudFront domain"
      pattern: "AliasTarget.*CloudFront"
---

<objective>
Establish production DNS routing via Route53 and CDN edge caching via CloudFront for resilience, DDoS protection, and performance.

Purpose: Production traffic routes through CloudFront (edge caching, DDoS mitigation) instead of directly to API Gateway. Route53 health checks validate API Gateway availability and can support failover logic in future phases. This layer provides cost-conscious resilience per Phase 07.1 decisions.

Output: CloudFront distribution configured with API Gateway origin, Route53 health checks and DNS records, operator documentation.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.planning/phases/07.1-production-infrastructure-enablement/07.1-CONTEXT.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-deployment-foundation-and-promotion-path/06-01-SUMMARY.md (API Gateway endpoint reference)
@.planning/phases/07.1-production-infrastructure-enablement/07.1-CONTEXT.md (DNS and Routing, CloudFront decisions)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Route53 health check and DNS record pointing to CloudFront origin</name>
  <files>template.yaml</files>
  <read_first>
    - template.yaml (to understand how to reference API Gateway endpoint)
    - deploy/params.staging.json (reference for parameter naming)
    - .planning/phases/07.1-production-infrastructure-enablement/07.1-CONTEXT.md (Route53 design decisions)
  </read_first>
  <action>
Add to template.yaml Parameters section:
- Route53HostedZoneId (String, Description: "Route53 hosted zone for production domain", e.g., "Z123ABCD...")
- ProductionDomainName (String, Description: "Production domain (e.g., api.example.com)")
- HealthCheckInterval (Number, Default: 30, AllowedValues: [10, 30] - seconds between checks)
- HealthCheckFailureThreshold (Number, Default: 3, minimum failures to mark unhealthy)

Add to template.yaml Resources section:

```yaml
  ProductionApiHealthCheck:
    Type: AWS::Route53::HealthCheck
    Condition: IsProduction
    Properties:
      Type: HTTPS
      IPAddress: !Sub "{{API_GATEWAY_ENDPOINT}}" # Will be resolved from CloudFormation reference
      Port: 443
      ResourcePath: /health  # API Gateway endpoint paths depend on Lambda configuration
      RequestInterval: !Ref HealthCheckInterval
      FailureThreshold: !Ref HealthCheckFailureThreshold
      MeasureLatency: true
      EnableSNI: true
      FullyQualifiedDomainName: !Sub "{{API_GATEWAY_DOMAIN}}"
      Tags:
        - Key: Name
          Value: nunti-production-api-health
        - Key: Environment
          Value: production

  ProductionRoute53HealthCheckAlarm:
    Type: AWS::CloudWatch::Alarm
    Condition: IsProduction
    Properties:
      AlarmName: nunti-production-api-health-down
      AlarmDescription: Alert when production API Gateway health check fails
      MetricName: HealthCheckStatus
      Namespace: AWS/Route53
      Statistic: Minimum
      Period: 60
      EvaluationPeriods: 2
      Threshold: 1
      ComparisonOperator: LessThanThreshold
      Dimensions:
        - Name: HealthCheckId
          Value: !Ref ProductionApiHealthCheck
      TreatMissingData: breaching

  ProductionRoute53DnsRecord:
    Type: AWS::Route53::RecordSet
    Condition: IsProduction
    Properties:
      HostedZoneId: !Ref Route53HostedZoneId
      Name: !Ref ProductionDomainName
      Type: A
      AliasTarget:
        DNSName: !GetAtt ProductionCloudFrontDistribution.DomainName
        HostedZoneId: Z2FDTNDATAQYW2  # CloudFront's hosted zone ID (constant)
        EvaluateTargetHealth: false
      HealthCheckId: !Ref ProductionApiHealthCheck
      SetIdentifier: primary
      Failover: PRIMARY
      Comment: Production DNS routing via CloudFront with health monitoring
      Tags:
        - Key: Environment
          Value: production
```

Note: API_GATEWAY_ENDPOINT and API_GATEWAY_DOMAIN will be replaced with actual references once API Gateway structure is known. For now, these are placeholders that deployer will fill via params or CloudFormation stack references.
  </action>
  <verify>
    <automated>grep -q "AWS::Route53::HealthCheck" template.yaml && grep -q "AWS::Route53::RecordSet" template.yaml && grep -q "HTTPS" template.yaml</automated>
  </verify>
  <done>template.yaml contains Route53 health check monitoring API Gateway on 30-second interval with 3-failure threshold, CloudWatch alarm for health check failures, and DNS record with alias routing to CloudFront. Health check supports automatic failover in future phases.</done>
</task>

<task type="auto">
  <name>Task 2: Create CloudFront distribution with API Gateway origin and cache configuration</name>
  <files>template.yaml</files>
  <read_first>
    - template.yaml (existing resources)
    - .planning/phases/07.1-production-infrastructure-enablement/07.1-CONTEXT.md (CloudFront cache and WAF decisions)
  </read_first>
  <action>
Add to template.yaml Parameters section:
- CloudFrontDefaultTTL (Number, Default: 300, seconds to cache responses)
- CloudFrontMaxTTL (Number, Default: 3600, maximum cache duration)
- CloudFrontMinTTL (Number, Default: 0, minimum cache duration)

Add to template.yaml Resources section:

```yaml
  ProductionCloudFrontDistribution:
    Type: AWS::CloudFront::Distribution
    Condition: IsProduction
    Properties:
      DistributionConfig:
        Enabled: true
        DefaultRootObject: /
        HttpVersion: http2and3
        GenerateETag: true
        
        # Origin configuration pointing to API Gateway
        Origins:
          - Id: ApiGatewayOrigin
            DomainName: !Sub "{{API_GATEWAY_DOMAIN}}"  # Placeholder for API Gateway endpoint
            CustomOriginConfig:
              HTTPPort: 80
              HTTPSPort: 443
              OriginProtocolPolicy: https-only
              OriginSSLProtocols:
                - TLSv1.2
            OriginAccessIdentity: ""  # Not using OAI for public API Gateway
            OriginShield:
              Enabled: true
              OriginShieldRegion: !Ref AWS::Region

        # Default cache behavior for all requests
        DefaultCacheBehavior:
          TargetOriginId: ApiGatewayOrigin
          AllowedMethods:
            - GET
            - HEAD
            - OPTIONS
            - PUT
            - POST
            - PATCH
            - DELETE
          CacheMethods:
            - GET
            - HEAD
            - OPTIONS
          ViewerProtocolPolicy: redirect-to-https
          CachePolicyId: "Managed-CachingDisabled"  # No caching for dynamic API endpoints
          OriginRequestPolicyId: "Managed-AllViewerExceptHostHeader"
          Compress: true
          FunctionAssociations:
            - EventType: viewer-request
              FunctionARN: !GetAtt CloudFrontSecurityHeadersFunction.FunctionMetadata.FunctionArn

        # WebSocket cache behavior (if Phase 8+ needs WebSocket specifically)
        CacheBehaviors:
          - PathPattern: "/ws/*"
            TargetOriginId: ApiGatewayOrigin
            AllowedMethods:
              - GET
              - HEAD
              - PUT
              - POST
              - PATCH
              - DELETE
            CacheMethods:
              - GET
              - HEAD
            ViewerProtocolPolicy: https-only
            CachePolicyId: "Managed-CachingDisabled"
            OriginRequestPolicyId: "Managed-AllViewer"
            Compress: false  # WebSocket doesn't benefit from compression

        # Security headers via CloudFront Function
        ViewerResponseFunctionAssociations:
          - EventType: viewer-response
            FunctionARN: !GetAtt CloudFrontSecurityHeadersFunction.FunctionMetadata.FunctionArn

        # Logging
        Logging:
          Bucket: !GetAtt CloudFrontLogsBucket.DomainName
          IncludeCookies: false
          Prefix: cloudfront-logs/

        # Price class (cost optimization: North America + Europe only)
        PriceClass: PriceClass_100

        # AWS Shield Standard (included free with CloudFront)
        WebACLId: !GetAtt ProductionWAF.Arn

        # Security headers and restrictions
        CustomErrorResponses:
          - ErrorCode: 403
            ResponseCode: 403
            ResponsePagePath: /error/403.html
            ErrorCachingMinTTL: 60
          - ErrorCode: 404
            ResponseCode: 404
            ResponsePagePath: /error/404.html
            ErrorCachingMinTTL: 300
          - ErrorCode: 500
            ResponseCode: 500
            ResponsePagePath: /error/500.html
            ErrorCachingMinTTL: 30

        Comment: Production CloudFront distribution for API Gateway with caching and DDoS protection
        Tags:
          - Key: Environment
            Value: production
          - Key: CDN
            Value: CloudFront

  CloudFrontSecurityHeadersFunction:
    Type: AWS::CloudFront::Function
    Condition: IsProduction
    Properties:
      AutoPublish: true
      FunctionCode: |
        function handler(event) {
            var response = event.response;
            var headers = response.headers;

            headers['strict-transport-security'] = {value: 'max-age=31536000; includeSubdomains; preload'};
            headers['x-content-type-options'] = {value: 'nosniff'};
            headers['x-frame-options'] = {value: 'DENY'};
            headers['x-xss-protection'] = {value: '1; mode=block'};
            headers['referrer-policy'] = {value: 'strict-origin-when-cross-origin'};
            headers['content-security-policy'] = {value: "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline';"};

            return response;
        }
      FunctionConfig:
        Comment: Add security headers to CloudFront responses
        Runtime: cloudfront-js-1.0
      Name: nunti-cloudfront-security-headers

  ProductionWAF:
    Type: AWS::WAFv2::WebACL
    Condition: IsProduction
    Properties:
      Name: nunti-production-waf
      Scope: CLOUDFRONT
      DefaultAction:
        Allow: {}
      Rules:
        # Rate limiting (100 requests per 5 minutes from single IP)
        - Name: RateLimitRule
          Priority: 0
          Action:
            Block: {}
          Statement:
            RateBasedStatement:
              Limit: 100
              AggregateKeyType: IP
          VisibilityConfig:
            SampledRequestsEnabled: true
            CloudWatchMetricsEnabled: true
            MetricName: RateLimitRule
        
        # AWS Managed Rules - Core Rule Set
        - Name: AWSManagedRulesCommonRuleSet
          Priority: 1
          OverrideAction:
            None: {}
          Statement:
            ManagedRuleGroupStatement:
              VendorName: AWS
              Name: AWSManagedRulesCommonRuleSet
          VisibilityConfig:
            SampledRequestsEnabled: true
            CloudWatchMetricsEnabled: true
            MetricName: AWSManagedRulesCommonRuleSetMetric

      VisibilityConfig:
        SampledRequestsEnabled: true
        CloudWatchMetricsEnabled: true
        MetricName: nunti-production-waf-metrics
      Tags:
        - Key: Environment
          Value: production

  CloudFrontLogsBucket:
    Type: AWS::S3::Bucket
    Condition: IsProduction
    Properties:
      BucketName: !Sub "nunti-cloudfront-logs-${AWS::AccountId}-production"
      AccessControl: Private
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        BlockPublicPolicy: true
        IgnorePublicAcls: true
        RestrictPublicBuckets: true
      VersioningConfiguration:
        Status: Enabled
      LifecycleConfiguration:
        Rules:
          - Id: DeleteOldLogs
            Status: Enabled
            ExpirationInDays: 90
            NoncurrentVersionExpirationInDays: 30
      Tags:
        - Key: Environment
          Value: production
        - Key: Purpose
          Value: CloudFront logging

  CloudFrontLogsS3Policy:
    Type: AWS::S3::BucketPolicy
    Condition: IsProduction
    Properties:
      Bucket: !Ref CloudFrontLogsBucket
      PolicyText:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: cloudfront.amazonaws.com
            Action: s3:PutObject
            Resource: !Sub "${CloudFrontLogsBucket.Arn}/*"
          - Effect: Allow
            Principal:
              Service: cloudfront.amazonaws.com
            Action: s3:GetBucketAcl
            Resource: !GetAtt CloudFrontLogsBucket.Arn
```

Add to Outputs section:
```yaml
  ProductionCloudFrontDomainName:
    Condition: IsProduction
    Value: !GetAtt ProductionCloudFrontDistribution.DomainName
    Export:
      Name: !Sub "nunti-cloudfront-domain-${Stage}"
  ProductionCloudFrontDistributionId:
    Condition: IsProduction
    Value: !Ref ProductionCloudFrontDistribution
    Export:
      Name: !Sub "nunti-cloudfront-distribution-id-${Stage}"
```

Key decisions:
- Cache-disabled for API responses (CachingDisabled policy) since API is dynamic
- PriceClass_100 limits CloudFront to North America + Europe + Middle East (cost-conscious)
- WAF includes rate limiting (100 req/5min per IP) and AWS Managed Core Rules
- Security headers added via CloudFront Function (HSTS, CSP, X-Frame-Options, etc.)
- CloudFront Logs stored in S3 with 90-day expiration
  </action>
  <verify>
    <automated>grep -q "AWS::CloudFront::Distribution" template.yaml && grep -q "ProductionCloudFrontDistribution" template.yaml && grep -q "AWS::WAFv2::WebACL" template.yaml && grep -q "strict-transport-security" template.yaml</automated>
  </verify>
  <done>template.yaml contains CloudFront distribution with API Gateway origin, cache-disabled policy for dynamic content, security headers via CloudFront Function, WAF with rate limiting and managed rules, S3 logging bucket with lifecycle. Outputs export CloudFront domain and distribution ID for Route53 routing.</done>
</task>

<task type="auto">
  <name>Task 3: Update deploy/params.production.json with Route53 and CloudFront parameters, document DNS setup</name>
  <files>deploy/params.production.json, docs/deployment/dns-and-cdn-setup.md</files>
  <read_first>
    - deploy/params.production.json (existing parameters)
    - template.yaml (parameter names)
    - .planning/phases/07.1-production-infrastructure-enablement/07.1-CONTEXT.md (DNS routing design)
  </read_first>
  <action>
Update deploy/params.production.json to include:

```json
  "Route53HostedZoneId=Z123ABCDEFGHIJ",
  "ProductionDomainName=api.example.com",
  "HealthCheckInterval=30",
  "HealthCheckFailureThreshold=3",
  "CloudFrontDefaultTTL=300",
  "CloudFrontMaxTTL=3600",
  "CloudFrontMinTTL=0"
```

Add comments:
```
// DNS and CDN Configuration
// - Route53HostedZoneId: ID of your Route53 hosted zone (e.g., Z123ABCDEFGHIJ)
// - ProductionDomainName: Production domain name (e.g., api.example.com, api-prod.myapp.io)
// - HealthCheckInterval: 30 seconds between Route53 health checks (10 or 30 only)
// - HealthCheckFailureThreshold: 3 consecutive failures to mark unhealthy
// - CloudFront cache TTLs: Disabled for dynamic API (0-300s recommended)
```

Create docs/deployment/dns-and-cdn-setup.md:

```markdown
# Production DNS and CDN Setup

## Architecture Overview

\`\`\`
Users
  ↓
Route53 (DNS resolution)
  ↓
CloudFront (CDN edge, caching, WAF, DDoS protection)
  ↓
API Gateway (ALB equivalent in serverless)
  ↓
Lambda (compute layer in VPC)
  ↓
DynamoDB (data layer)
\`\`\`

## Route53 Setup

### Prerequisites

1. **Hosted Zone:** Your domain must be registered and have Route53 hosted zone created
   - Example: \`example.com\` with hosted zone ID \`Z123ABCDEFGHIJ\`

2. **Nameservers:** Update domain registrar to point to Route53 nameservers
   - AWS Route53 Console → Hosted Zones → (select zone) → Copy NS record values
   - Update domain registrar nameserver settings to these Route53 nameservers

### Health Check Configuration

Route53 health check monitors API Gateway every 30 seconds:

\`\`\`bash
aws route53 describe-health-check \
  --health-check-id {{HEALTH_CHECK_ID}} \
  --query 'HealthCheck.[HealthCheckConfig, HealthCheckObservations]'
\`\`\`

If health check fails:
1. Route53 marks endpoint unhealthy
2. CloudWatch alarm triggers
3. Future phases (Phase 8+) can implement automatic failover

**Manual failover test:**
1. Stop API Gateway or Lambda concurrency
2. Observe Route53 health check fail in ~90 seconds (3 failures × 30sec)
3. Update DNS record to failover destination (if configured)
4. Resume API Gateway

### Updating DNS Records

To point production domain to CloudFront:

\`\`\`bash
aws route53 change-resource-record-sets \
  --hosted-zone-id Z123ABCDEFGHIJ \
  --change-batch file://dns-update.json
\`\`\`

Where \`dns-update.json\` contains:
\`\`\`json
{
  "Changes": [
    {
      "Action": "UPSERT",
      "ResourceRecordSet": {
        "Name": "api.example.com",
        "Type": "A",
        "AliasTarget": {
          "HostedZoneId": "Z2FDTNDATAQYW2",
          "DNSName": "d111111abcdef8.cloudfront.net",
          "EvaluateTargetHealth": true
        }
      }
    }
  ]
}
\`\`\`

## CloudFront Configuration

### Cache Behavior

Production API endpoints are **cache-disabled** (no caching) because:
- Message delivery must reflect real-time state
- Caching stale data could cause message loss or duplication
- Dynamic WebSocket endpoints cannot be cached

Cache behavior per endpoint:

| Endpoint | Cache Policy | TTL | Reason |
|----------|--------------|-----|--------|
| /ws/* | Disabled | N/A | WebSocket is bidirectional, no HTTP caching |
| /v1/auth/* | Disabled | N/A | Auth tokens are sensitive, always fresh |
| /v1/messages/* | Disabled | N/A | Message delivery must be real-time |
| /health | Cached | 60s | Health check can be slightly stale |

### Distributions Status

View CloudFront distribution status:

\`\`\`bash
aws cloudfront get-distribution-config \
  --id {{DISTRIBUTION_ID}} \
  --query 'DistributionConfig.[Enabled, Status, DomainName, Origins]'
\`\`\`

Monitor CloudFront metrics in CloudWatch:

\`\`\`bash
aws cloudwatch get-metric-statistics \
  --namespace AWS/CloudFront \
  --metric-name Requests \
  --dimensions Name=DistributionId,Value={{DISTRIBUTION_ID}} \
  --statistics Sum \
  --start-time 2026-04-10T00:00:00Z \
  --end-time 2026-04-10T23:59:59Z \
  --period 3600
\`\`\`

### Cache Invalidation

If content needs to be refreshed before TTL expires:

\`\`\`bash
aws cloudfront create-invalidation \
  --distribution-id {{DISTRIBUTION_ID}} \
  --paths "/*"
\`\`\`

This invalidates all cached objects (useful after deployments). **Cost:** First 1,000 invalidations free/month, then $0.005 each. Use sparingly.

## WAF Configuration

### Rate Limiting

CloudFront WAF enforces rate limit:
- **Limit:** 100 requests per 5 minutes per IP
- **Action:** Block (return 403)
- **Bypass:** Only possible via AWS support (emergency override)

Check WAF metrics:

\`\`\`bash
aws cloudwatch get-metric-statistics \
  --namespace AWS/WAFV2 \
  --metric-name BlockedRequests \
  --dimensions Name=Rule,Value=RateLimitRule \
  --statistics Sum \
  --start-time 2026-04-10T00:00:00Z \
  --end-time 2026-04-10T23:59:59Z
\`\`\`

### DDoS Protection

CloudFront includes AWS Shield Standard (free):
- Protects against common Layer 3/4 DDoS attacks
- Automatic mitigation for up to 100 Gbps+ attacks
- No configuration required

Advanced DDoS protection (AWS Shield Advanced) deferred to Phase 11.

## Logging and Monitoring

### CloudFront Logs

All CloudFront requests logged to S3:
- **Bucket:** \`nunti-cloudfront-logs-{account-id}-production\`
- **Path:** \`cloudfront-logs/\`
- **Retention:** 90 days (auto-deleted)
- **Format:** Tab-separated values (TSV), gzipped

Analyze logs:

\`\`\`bash
aws s3 cp s3://nunti-cloudfront-logs-{account-id}-production/cloudfront-logs/2026-04-10-*.gz .
gunzip *.gz
awk -F'\t' '{print $4, $5}' *.log | sort | uniq -c | sort -rn | head -20
\`\`\`

This shows top 20 URLs by request count.

### Health Check Alarming

CloudWatch alarm triggers if health check fails:
- **Alarm:** \`nunti-production-api-health-down\`
- **Threshold:** 2 consecutive failures (2 × 30sec = 60 sec total)
- **Action:** SNS notification (configure in Phase 11)

Check current alarm status:

\`\`\`bash
aws cloudwatch describe-alarms --alarm-names nunti-production-api-health-down
\`\`\`

## Troubleshooting

### DNS Resolution Issues

If \`api.example.com\` doesn't resolve:

1. **Check Route53 record exists:**
   \`\`\`bash
   aws route53 list-resource-record-sets \
     --hosted-zone-id Z123ABCDEFGHIJ \
     --query 'ResourceRecordSets[?Name==\`api.example.com\`]'
   \`\`\`

2. **Verify nameservers:**
   \`\`\`bash
   nslookup api.example.com
   dig api.example.com NS
   \`\`\`

3. **Clear local DNS cache:**
   - macOS: \`sudo dscacheutil -flushcache\`
   - Linux: \`sudo systemctl restart systemd-resolved\`
   - Windows: \`ipconfig /flushdns\`

### CloudFront Slow Response

If CloudFront is slow:

1. **Check origin health:** Is API Gateway responding?
   \`\`\`bash
   curl -I https://{{API_GATEWAY_DOMAIN}}/ -H "Host: api.example.com"
   \`\`\`

2. **Check CloudFront metrics:** Request count, error rate, latency
   - AWS Console → CloudFront → Monitoring → Distributions

3. **Invalidate cache:** Force refresh
   \`\`\`bash
   aws cloudfront create-invalidation --distribution-id {{ID}} --paths "/*"
   \`\`\`

### Health Check Failures

If Route53 marks API healthy but CloudFront still fails:

1. **Verify origin endpoint security group** allows CloudFront IPs
   - CloudFront uses multiple edge locations; allow all CloudFront IPs via
   - AWS Console → VPC → Security Groups or use \`amazon.com\` prefix list

2. **Check API Gateway integration:**
   - API Gateway must support HTTPS on port 443
   - SNI (Server Name Indication) must be enabled
   - Certificate must be valid and not expired

3. **Monitor API Gateway logs:**
   - AWS Console → API Gateway→ (select API) → Logs
   - Check for 5xx errors correlating with health check failures

## DNS Migration Checklist

When moving production to Route53 + CloudFront:

- [ ] Route53 hosted zone created
- [ ] Nameservers updated at domain registrar
- [ ] DNS propagation verified (2-48 hours typical)
- [ ] CloudFront distribution created and deployed
- [ ] Health check is passing consistently
- [ ] Route53 DNS record points to CloudFront
- [ ] SSL/TLS certificate valid on API Gateway
- [ ] WAF rules are active (check CloudWatch metrics)
- [ ] CloudFront logs flowing to S3
- [ ] Operator is monitoring health check alarm

## Phase Continuance

Future phases (Phase 8+) will enhance:
- Automatic failover routing
- Multi-endpoint load balancing
- Canary deployments via weighted routing
- Custom health check conditions
```
  </action>
  <verify>
    <automated>grep -q "Route53HostedZoneId" deploy/params.production.json && test -f docs/deployment/dns-and-cdn-setup.md && grep -q "CloudFront" docs/deployment/dns-and-cdn-setup.md && grep -q "Route53" docs/deployment/dns-and-cdn-setup.md</automated>
  </verify>
  <done>deploy/params.production.json includes Route53 zone ID, domain name, and health check parameters. Created comprehensive dns-and-cdn-setup.md with Route53 nameserver setup, health check monitoring, CloudFront cache behavior, WAF rate limiting, logging analysis, troubleshooting guide, and DNS migration checklist for operators.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → CloudFront | Untrusted traffic; CloudFront is first defense |
| CloudFront → API Gateway | Internal AWS edge-to-origin; TLS 1.2+ enforced |
| Route53 Health | Health check endpoints must be publicly accessible (HTTPS) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.1-21 | Spoofing | DNS records | mitigate | "Route53 is AWS managed; DNS hijacking requires account compromise (mitigated by IAM). DNSSEC deferred to Phase 11." |
| T-07.1-22 | Tampering | CloudFront distribution | mitigate | "Distribution config protected by IAM. Only authorized operators can modify. CloudFront distribution policy uses HTTPS-only to origin." |
| T-07.1-23 | Information Disclosure | CloudFront logs | mitigate | "Logs stored in S3 with encryption at rest (AWS managed). Access controlled via bucket policy. Auto-deleted after 90 days." |
| T-07.1-24 | Denial of Service | CloudFront | mitigate | "AWS Shield Standard included (automatic). Rate limiting via WAF (100 req/5sec per IP). Advanced Shield deferred to Phase 11." |
| T-07.1-25 | Denial of Service | Health check | accept | "Health check endpoint is public (must be for Route53 access). Flooding health check could trigger false failover. Mitigated by health check interval (30s) and failure threshold (3 consecutive)." |
| T-07.1-26 | Authorization | Distribution access | mitigate | "CloudFront origin is restricted to CloudFront edge locations via origin security group. Direct API Gateway access requires explicit IP allowlisting." |

</threat_model>

<verification>
After task completion:
1. Validate template.yaml syntax: `sam validate template.yaml`
2. Verify params.production.json: `jq .ParameterOverrides deploy/params.production.json | grep -i route53`
3. Grep for CloudFront and Route53 resource definitions
4. Verify WAF rule configuration in template
5. Check S3 bucket policy for CloudFront logs
</verification>

<success_criteria>
- [x] template.yaml contains Route53 health check monitoring API Gateway
- [x] CloudFront distribution created with API Gateway as origin
- [x] CloudFront cache policy is disabled/minimal for dynamic API endpoints
- [x] WAF included with rate limiting (100 req/5min per IP) and managed rules
- [x] CloudFront Function adds security headers (HSTS, CSP, X-Frame-Options)
- [x] CloudFront logs stored in S3 with 90-day retention
- [x] Route53 DNS record points to CloudFront distribution (alias target)
- [x] deploy/params.production.json includes Route53 zone ID and domain parameters
- [x] dns-and-cdn-setup.md provides complete operator guide: DNS setup, health monitoring, cache invalidation, WAF configuration, troubleshooting, migration checklist
- [x] CloudWatch alarm configured for health check failures
- [x] AWS Shield Standard DDoS protection automatically enabled
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-production-infrastructure-enablement/07.1-04-SUMMARY.md`
</output>
