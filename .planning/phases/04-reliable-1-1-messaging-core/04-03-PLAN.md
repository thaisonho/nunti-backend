---
phase: 04-reliable-1-1-messaging-core
plan: 03
type: execute
wave: 3
depends_on:
  - "04-02"
files_modified:
  - src/messages/message-repository.ts
  - src/messages/message-service.ts
  - src/realtime/message-relay-publisher.ts
  - src/handlers/ws/reconnect.ts
  - src/handlers/ws/messages.ts
  - tests/unit/message-replay.test.ts
  - tests/integration/messages-reconnect.test.ts
autonomous: true
requirements:
  - MSG-03
must_haves:
  truths:
    - "A reconnecting device receives every retained queued message in strict oldest-first order by server enqueue time."
    - "The backend drains backlog replay before switching the connection back to live traffic."
    - "A replay-complete event marks the end of catch-up so the client can resume normal message processing."
  artifacts:
    - path: src/messages/message-repository.ts
      provides: "Oldest-first queued inbox query and replay update primitives"
    - path: src/messages/message-service.ts
      provides: "Reconnect replay orchestration and backlog drain state machine"
    - path: src/handlers/ws/reconnect.ts
      provides: "WebSocket reconnect route that triggers backlog replay"
    - path: src/realtime/message-relay-publisher.ts
      provides: "Replay-complete and backlog-drain event publishing"
    - path: tests/integration/messages-reconnect.test.ts
      provides: "Reconnect replay and completion coverage"
  key_links:
    - from: src/handlers/ws/reconnect.ts
      to: src/messages/message-service.ts
      via: "reconnect-triggered backlog replay request"
      pattern: "replay|reconnect"
    - from: src/messages/message-service.ts
      to: src/messages/message-repository.ts
      via: "oldest-first queued inbox query and delivery-state update"
      pattern: "serverTimestamp|queued|delivered"
    - from: src/messages/message-service.ts
      to: src/realtime/message-relay-publisher.ts
      via: "replay-complete and backlog-drain status events"
      pattern: "replay-complete|drain|live"
---

<objective>
Add reconnect recovery for queued 1:1 messages.

Purpose: Phase 4 must not lose messages when the recipient device was offline; reconnect replay closes that gap and makes the direct-message core deterministic.
Output: Reconnect replay handler, oldest-first queued-message drain, and replay-complete signaling that hands the connection back to live traffic.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-reliable-1-1-messaging-core/04-CONTEXT.md
@src/app/http-response.ts
@src/app/errors.ts
</context>

<strategy>
Treat replay as a strict backlog-drain step: query all retained queued messages in server order, deliver them before live traffic, and then emit a dedicated replay-complete signal. Keep the replay path separate from the normal send path so the state machine stays deterministic.
</strategy>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement queued backlog query and reconnect replay orchestration</name>
  <files>src/messages/message-repository.ts, src/messages/message-service.ts, src/handlers/ws/reconnect.ts, tests/unit/message-replay.test.ts, tests/integration/messages-reconnect.test.ts</files>
  <behavior>
    - The replay query returns every retained queued message for the reconnecting device in strict oldest-first order.
    - Replay drains the entire backlog, not a sample or a recent subset.
    - Delivered replay items are removed or marked delivered so they are not replayed again.
  </behavior>
  <action>
    Add the reconnect replay query and orchestration so the service can read the queued inbox, deliver each message in order, and update message state as each item is drained.

    Reuse the state and persistence primitives from the prior waves rather than inventing a second replay store. Keep the replay logic isolated from the normal send path so the client sees a clear backlog phase before live messages resume.
  </action>
  <verify>
    <automated>npm test -- tests/unit/message-replay.test.ts tests/integration/messages-reconnect.test.ts</automated>
  </verify>
  <done>
    Reconnecting devices receive queued messages in deterministic order and the backlog is fully drained before the replay step completes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit replay-complete and return the connection to live traffic</name>
  <files>src/messages/message-service.ts, src/realtime/message-relay-publisher.ts, src/handlers/ws/messages.ts, tests/integration/messages-reconnect.test.ts</files>
  <behavior>
    - A replay-complete event is emitted exactly once when backlog catch-up finishes.
    - After replay-complete, the connection resumes normal live message handling.
    - Replay does not leak into normal send semantics or duplicate sender-facing outcomes.
  </behavior>
  <action>
    Wire the end-of-replay state so the service emits an explicit replay-complete event, then allows the connection to continue with live traffic. Keep the event flat and machine-readable, matching the phase decision to use eventType plus stable identifiers and request correlation fields where needed.

    This task should only finalize the reconnect state machine; do not reopen the retry-idempotency rules or change the live send contract again.
  </action>
  <verify>
    <automated>npm test -- tests/integration/messages-reconnect.test.ts</automated>
  </verify>
  <done>
    Reconnect catch-up has a clear terminal signal and normal live delivery resumes after the backlog is exhausted.</done>
</task>

</tasks>

<verification>
Run the reconnect slice first, then the full repository suite before handing the plan to execution:

- `npm test -- tests/unit/message-replay.test.ts tests/integration/messages-reconnect.test.ts`
- `npm test`

Wave 3 is ready only when backlog replay is deterministic and the client receives an explicit replay-complete boundary before live traffic resumes.
</verification>

<success_criteria>
Offline 1:1 messaging recovery works: retained queued messages replay in order on reconnect, the backlog is fully drained, and the client receives a distinct replay-complete signal before normal traffic continues.
</success_criteria>

<output>
After completion, create `.planning/phases/04-reliable-1-1-messaging-core/04-03-SUMMARY.md`.
</output>