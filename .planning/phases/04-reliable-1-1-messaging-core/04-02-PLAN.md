---
phase: 04-reliable-1-1-messaging-core
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/messages/message-repository.ts
  - src/messages/message-service.ts
  - src/realtime/message-relay-publisher.ts
  - tests/unit/message-repository.test.ts
  - tests/unit/message-service.test.ts
  - tests/integration/messages-dedup.test.ts
  - tests/integration/messages-failure.test.ts
autonomous: true
requirements:
  - MSG-02
must_haves:
  truths:
    - "Retrying a send with the same messageId returns the prior outcome instead of creating duplicate queue items or duplicate relay side effects."
    - "Sender-facing delivery acknowledgements reflect the real delivery state and stay stable across retries."
    - "Queued messages that exceed the retention policy become terminal failed outcomes tied to messageId."
  artifacts:
    - path: src/messages/message-repository.ts
      provides: "Conditional persistence and delivery-state update primitives keyed by messageId"
    - path: src/messages/message-service.ts
      provides: "Idempotent send and status-transition orchestration"
    - path: src/realtime/message-relay-publisher.ts
      provides: "Sender-facing accepted/delivered/failed event publishing"
    - path: tests/unit/message-repository.test.ts
      provides: "Conditional-write and duplicate-send storage coverage"
    - path: tests/integration/messages-dedup.test.ts
      provides: "Idempotent retry behavior and duplicate suppression coverage"
  key_links:
    - from: src/messages/message-service.ts
      to: src/messages/message-repository.ts
      via: "messageId conditional write and state transition updates"
      pattern: "TransactWriteCommand|ConditionExpression|UpdateCommand"
    - from: src/messages/message-service.ts
      to: src/realtime/message-relay-publisher.ts
      via: "sender-facing delivery outcome events"
      pattern: "accepted|delivered|failed"
    - from: tests/integration/messages-dedup.test.ts
      to: src/messages/message-service.ts
      via: "same messageId retry assertions"
      pattern: "messageId|duplicate|retry"
---

<objective>
Make direct-message sends retry-safe and delivery-state aware.

Purpose: Phase 4 cannot be reliable until a repeated client send is recognized as the same message and the backend suppresses duplicate queue or relay side effects.
Output: Idempotent message persistence, sender-facing delivery acknowledgements, and terminal failure handling for queued messages that age out.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-reliable-1-1-messaging-core/04-CONTEXT.md
@src/app/config.ts
@src/app/http-response.ts
@src/app/errors.ts
</context>

<strategy>
Use the messageId as the idempotency key and keep the delivery-state transitions in the service layer. This wave should not add reconnect replay yet; it only needs to make accepted, delivered, and failed outcomes stable under duplicate sends and retention expiry.
</strategy>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add idempotent message persistence and retry suppression</name>
  <files>src/messages/message-repository.ts, src/messages/message-service.ts, tests/unit/message-repository.test.ts, tests/unit/message-service.test.ts, tests/integration/messages-dedup.test.ts</files>
  <behavior>
    - The same messageId sent again returns the stored outcome instead of creating a second queue row or a second relay attempt.
    - Conditional persistence keeps message creation atomic so concurrent retries do not split the state.
    - Message delivery state updates remain consistent with the stored canonical record.
  </behavior>
  <action>
    Upgrade the first-pass message repository into a retry-safe persistence layer keyed by messageId. Use conditional writes or an equivalent atomic pattern so the service can detect duplicate sends, read back the stored outcome, and skip duplicate side effects.

    Keep the repository focused on storage and retrieval primitives while the service owns the state-machine decisions. Preserve the phase contract that a retry returns the same logical result rather than a new message instance.
  </action>
  <verify>
    <automated>npm test -- tests/unit/message-repository.test.ts tests/unit/message-service.test.ts tests/integration/messages-dedup.test.ts</automated>
  </verify>
  <done>
    Duplicate sends are treated as retries of the same messageId, not as new messages, and the backend no longer duplicates queue side effects.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit stable delivery-failed outcomes for expired queued messages</name>
  <files>src/messages/message-service.ts, src/realtime/message-relay-publisher.ts, tests/integration/messages-failure.test.ts</files>
  <behavior>
    - When queued delivery cannot complete within the retention policy, the backend emits a terminal failed event tied to the messageId.
    - The failed state is visible to the sender and does not continue to retry forever.
    - Live relay behavior from the prior wave remains unchanged for active recipients.
  </behavior>
  <action>
    Add the retention-policy check that turns stale queued messages into a terminal failure and publishes the failed outcome to the sender. Keep the policy constant local to the messaging service unless the implementation genuinely needs a wider configuration surface.

    Do not add a background scheduler here; the phase only needs deterministic failure handling when the service encounters an expired queued message. Keep the publisher payload aligned with the locked structured error/status semantics.
  </action>
  <verify>
    <automated>npm test -- tests/integration/messages-failure.test.ts</automated>
  </verify>
  <done>
    Expired queued messages no longer stay in limbo; they resolve to a terminal failed outcome with a stable messageId reference.</done>
</task>

</tasks>

<verification>
Run the retry-safety slice first, then the full repository suite before handing the plan to execution:

- `npm test -- tests/unit/message-repository.test.ts tests/unit/message-service.test.ts tests/integration/messages-dedup.test.ts tests/integration/messages-failure.test.ts`
- `npm test`

Wave 2 is ready only when duplicate sends are suppressed and terminal failure outcomes are stable under retention expiry.
</verification>

<success_criteria>
Message sends are now retry-safe: the same messageId cannot create duplicate side effects, sender-facing delivery states are stable, and expired queued messages resolve to a terminal failure instead of staying pending forever.
</success_criteria>

<output>
After completion, create `.planning/phases/04-reliable-1-1-messaging-core/04-02-SUMMARY.md`.
</output>