---
phase: 02-identity-and-device-access
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/devices/device-model.ts
  - src/devices/device-repository.ts
  - src/devices/device-service.ts
  - src/devices/device-policy.ts
  - src/handlers/http/devices-register.ts
  - src/handlers/http/devices-list.ts
  - src/handlers/http/devices-revoke.ts
  - src/handlers/http/me.ts
  - tests/unit/device-service.test.ts
  - tests/integration/protected-route-auth.test.ts
  - tests/integration/devices-flow.test.ts
autonomous: true
requirements:
  - AUTH-02
  - AUTH-03
must_haves:
  truths:
    - User can register multiple trusted devices tied to the authenticated account.
    - User can list active and revoked devices with basic visibility fields.
    - Revoked device is no longer authorized for protected device-sensitive participation.
    - Protected routes accept valid JWT and reject invalid JWT with locked 401/403 machine-code semantics.
  artifacts:
    - path: src/devices/device-service.ts
      provides: Device registration/list/revocation business rules for trusted participation
    - path: src/devices/device-repository.ts
      provides: DynamoDB persistence for per-user device lifecycle records
    - path: src/handlers/http/devices-revoke.ts
      provides: Revocation endpoint that marks device as revoked without hard deletion
    - path: src/handlers/http/me.ts
      provides: Protected-route auth acceptance/rejection probe endpoint
    - path: tests/integration/devices-flow.test.ts
      provides: End-to-end proof of register/list/revoke and post-revoke denial
  key_links:
    - from: src/handlers/http/devices-register.ts
      to: src/auth/auth-guard.ts
      via: authenticated user identity extracted before device trust mutation
      pattern: requireAuth
    - from: src/devices/device-service.ts
      to: src/devices/device-repository.ts
      via: register/list/revoke methods with status transitions trusted->revoked
      pattern: trusted|revoked|revokedAt|lastSeenAt
    - from: src/devices/device-policy.ts
      to: src/handlers/http/me.ts
      via: device authorization check gating protected participation after JWT validation
      pattern: AUTH_FORBIDDEN|status
---

<objective>
Implement trusted-device lifecycle and protected-route participation checks so users can control which devices remain authorized.

Purpose: Complete AUTH-03 and finalize Phase 2 success criteria by combining JWT auth validity with device trust enforcement.
Output: Device domain modules, register/list/revoke endpoints, protected participation probe route, and integration tests for revoke denial and token acceptance/rejection behavior.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-identity-and-device-access/02-CONTEXT.md
@.planning/phases/02-identity-and-device-access/02-RESEARCH.md
@.planning/phases/02-identity-and-device-access/02-VALIDATION.md
@.planning/phases/02-identity-and-device-access/02-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build trusted-device persistence and lifecycle rules</name>
  <read_first>
    - .planning/phases/02-identity-and-device-access/02-RESEARCH.md
    - src/auth/auth-guard.ts
    - tests/unit/device-service.test.ts
  </read_first>
  <files>src/devices/device-model.ts, src/devices/device-repository.ts, src/devices/device-service.ts, src/devices/device-policy.ts, tests/unit/device-service.test.ts</files>
  <behavior>
    - First successful sign-in registration writes trusted device record with registeredAt and lastSeenAt.
    - Multiple devices per user are supported without forcing sign-out of other devices.
    - Revoke updates status to revoked with revokedAt and blocks future trusted-device checks.
    - Listing returns basic visibility fields per locked decision (status, device label/metadata, lastSeenAt).
  </behavior>
  <action>Define device entity and status transition model for DynamoDB-backed records keyed per user+device. Implement repository operations for create/update/list with conditional semantics that keep revocation idempotent and auditable. Implement service rules that auto-register first successful device when invoked from signin flow and allow multiple active devices. Add policy helper that evaluates whether a device is currently trusted for participation. Keep scope strictly to auth/device trust; do not include profile search/discovery logic.</action>
  <verify>
    <automated>npm run test:auth -- tests/unit/device-service.test.ts</automated>
  </verify>
  <acceptance_criteria>Unit tests prove trusted and revoked state transitions, multi-device support, and policy-deny behavior for revoked devices.</acceptance_criteria>
  <done>Device domain modules persist and enforce trusted versus revoked lifecycle transitions with passing unit tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose authenticated device register/list/revoke endpoints and protected me probe route</name>
  <read_first>
    - src/auth/auth-guard.ts
    - src/auth/auth-error-mapper.ts
    - src/devices/device-service.ts
    - tests/integration/protected-route-auth.test.ts
    - tests/integration/devices-flow.test.ts
  </read_first>
  <files>src/handlers/http/devices-register.ts, src/handlers/http/devices-list.ts, src/handlers/http/devices-revoke.ts, src/handlers/http/me.ts, tests/integration/protected-route-auth.test.ts, tests/integration/devices-flow.test.ts</files>
  <behavior>
    - POST /v1/devices/register registers or refreshes trusted device for authenticated user.
    - GET /v1/devices returns device list scoped to authenticated user.
    - POST /v1/devices/{deviceId}/revoke revokes only caller-owned device and returns forbidden for cross-user attempt.
    - GET /v1/me accepts valid JWT and rejects invalid claims with locked machine-code contract.
  </behavior>
  <action>Implement device handlers using shared auth guard and response envelope from plan 02-01. Enforce locked rejection contract exactly: 401 for missing/invalid/expired token, 403 only for permission denials such as cross-user revoke or revoked-device participation checks. Ensure missing-token and malformed-token external message is identical. Include me endpoint as protected-route probe to verify valid-token acceptance path and invalid-token rejection path under AUTH-02.</action>
  <verify>
    <automated>npm run test:auth -- tests/integration/protected-route-auth.test.ts tests/integration/devices-flow.test.ts</automated>
  </verify>
  <acceptance_criteria>Integration tests confirm valid JWT acceptance, invalid JWT 401 mapping, cross-user revoke 403 mapping, and revoked devices denied participation on protected route checks.</acceptance_criteria>
  <done>Device and protected-route handlers enforce ownership and locked 401/403 semantics with passing integration tests.</done>
</task>

<task type="auto">
  <name>Task 3: Add phase verification matrix update for AUTH success criteria closure</name>
  <read_first>
    - .planning/phases/02-identity-and-device-access/02-VALIDATION.md
    - tests/integration/protected-route-auth.test.ts
    - tests/integration/devices-flow.test.ts
  </read_first>
  <files>.planning/phases/02-identity-and-device-access/02-VALIDATION.md</files>
  <action>Update validation matrix statuses and commands so AUTH-01, AUTH-02, and AUTH-03 are explicitly traceable to implemented unit/integration tests and a single phase gate command. Mark wave-0-complete and nyquist_compliant true only when all referenced commands are green. Keep manual-only verification limited to external Cognito delivery behavior.</action>
  <verify>
    <automated>rg -n "AUTH-01|AUTH-02|AUTH-03|nyquist_compliant: true|wave_0_complete: true" .planning/phases/02-identity-and-device-access/02-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>Validation document objectively maps all Phase 2 success criteria to executable commands and indicates readiness for phase verification without ambiguity.</acceptance_criteria>
  <done>Validation matrix is updated to green-trace all AUTH requirements with explicit commands and completion flags.</done>
</task>

</tasks>

<verification>
Run consolidated Phase 2 verification:
- npm run test:auth
- npm test
- rg -n "AUTH_TOKEN_MISSING_OR_MALFORMED|AUTH_TOKEN_EXPIRED|AUTH_TOKEN_INVALID_CLAIMS|AUTH_FORBIDDEN" src tests
</verification>

<success_criteria>
Users can manage trusted devices and revoked devices are blocked from participation, while protected routes consistently enforce locked JWT rejection semantics and all AUTH requirements are test-traceable.
</success_criteria>

<output>
After completion, create .planning/phases/02-identity-and-device-access/02-02-SUMMARY.md
</output>
