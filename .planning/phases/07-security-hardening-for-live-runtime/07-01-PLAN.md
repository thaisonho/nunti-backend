---
phase: 07-security-hardening-for-live-runtime
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/infra/setup-oidc-s3.sh
  - .github/workflows/release-deploy.yml
  - .github/workflows/release-promote.yml
  - .github/workflows/release-rollback.yml
  - template.yaml
  - docs/deployment/environment-configs.md
autonomous: true
requirements:
  - SEC-01
must_haves:
  truths:
    - "Staging and production deploy roles are separate and the normal bootstrap path does not grant AdministratorAccess."
    - "OIDC trust only allows the release workflows or release tags needed to assume the deploy roles."
    - "Runtime IAM only grants the AWS actions and resource scopes the live stack actually needs."
    - "Promotion and rollback workflows use the correct environment-specific role ARN."
  artifacts:
    - path: scripts/infra/setup-oidc-s3.sh
      provides: "OIDC bootstrap script with constrained role creation and explicit break-glass path"
    - path: template.yaml
      provides: "Least-privilege runtime IAM policy boundaries"
    - path: .github/workflows/release-promote.yml
      provides: "Production promotion wired to the production role ARN"
    - path: docs/deployment/environment-configs.md
      provides: "Operator guidance for the new role split"
  key_links:
    - from: scripts/infra/setup-oidc-s3.sh
      to: .github/workflows/release-deploy.yml
      via: "OIDC trust subject and role ARN output"
      pattern: "token\\.actions\\.githubusercontent\\.com:sub"
    - from: .github/workflows/release-promote.yml
      to: secrets.AWS_OIDC_ROLE_ARN_PROD
      via: "Production credential selection"
      pattern: "AWS_OIDC_ROLE_ARN_PROD"
    - from: template.yaml
      to: runtime Lambda permissions
      via: "Scoped IAM policy definitions"
      pattern: "Policies"
---

<objective>
Make live AWS deployment and runtime IAM defaults least-privilege by separating staging and production deploy roles, constraining OIDC trust, and encoding explicit runtime policy boundaries.

Purpose: Remove broad bootstrap permissions and make the live launch path production-safe by default.
Output: Hardened bootstrap script, updated workflows, scoped SAM policies, and operator docs.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From scripts/infra/setup-oidc-s3.sh:
```bash
ATTACH_ADMIN_ACCESS="${ATTACH_ADMIN_ACCESS:-false}"
```

From .github/workflows/release-promote.yml:
```yaml
role-to-assume: ${{ secrets.AWS_OIDC_ROLE_ARN }}
```

From .github/workflows/release-rollback.yml:
```yaml
role-to-assume: ${{ github.event.inputs.targetEnvironment == 'production' && secrets.AWS_OIDC_ROLE_ARN_PROD || secrets.AWS_OIDC_ROLE_ARN }}
```

From template.yaml:
```yaml
Globals:
  Function:
    Environment:
      Variables:
        COGNITO_USER_POOL_ID: !Ref CognitoUserPoolId
        COGNITO_APP_CLIENT_ID: !Ref CognitoAppClientId
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split deploy-role bootstrap and workflow credentials by environment</name>
  <files>scripts/infra/setup-oidc-s3.sh, .github/workflows/release-deploy.yml, .github/workflows/release-promote.yml, .github/workflows/release-rollback.yml</files>
  <action>Introduce distinct staging and production role creation paths, constrain the OIDC trust policy to release workflow/tag subjects only, and remove AdministratorAccess from the normal bootstrap path unless an explicit break-glass flag is set. Update the workflows to consume the correct role ARN for each environment so production promotion and rollback cannot silently reuse the staging credential.</action>
  <verify>bash -n scripts/infra/setup-oidc-s3.sh && node -e "const fs=require('fs'); for (const path of ['.github/workflows/release-deploy.yml','.github/workflows/release-promote.yml','.github/workflows/release-rollback.yml']) { const content=fs.readFileSync(path, 'utf8'); if (!content.includes('AWS_OIDC_ROLE_ARN')) throw new Error(path); }"</verify>
  <done>Staging and production deploy roles are no longer conflated, and the workflow credential paths reflect the environment split.</done>
</task>

<task type="auto">
  <name>Task 2: Encode least-privilege runtime IAM in the SAM template</name>
  <files>template.yaml</files>
  <action>Replace the placeholder stack structure with explicit IAM policy boundaries for the runtime and deployment path. Scope permissions to the live DynamoDB tables and required Lambda actions only, keep any broad access out of the normal path, and preserve break-glass usage as a documented exception rather than a default.</action>
  <verify>sam validate --template-file template.yaml</verify>
  <done>The template validates with explicit least-privilege policy structure instead of relying on broad default access.</done>
</task>

<task type="auto">
  <name>Task 3: Update operator documentation for the role split</name>
  <files>docs/deployment/environment-configs.md</files>
  <action>Document how the staging and production role ARNs are produced, which workflow uses which ARN, and that repository parameter JSON files remain placeholders with no committed secret values. Keep the instructions aligned with the hardened bootstrap flow.</action>
  <verify>git diff --check -- docs/deployment/environment-configs.md</verify>
  <done>The operator guide explains the new bootstrap and role-selection model without contradicting the workflow changes.</done>
</task>

</tasks>

<verification>
The phase is complete when the bootstrap script, workflow credential selection, SAM IAM boundaries, and operator docs all reflect a least-privilege deployment posture and no default admin bootstrap remains in the normal path.
</verification>

<success_criteria>
Production and staging use separate deploy roles, OIDC trust is narrowed to release subjects, the template validates with scoped IAM, and operators can follow the updated docs without needing broad bootstrap access.
</success_criteria>

<output>
After completion, create `.planning/phases/07-security-hardening-for-live-runtime/07-01-SUMMARY.md`
</output>