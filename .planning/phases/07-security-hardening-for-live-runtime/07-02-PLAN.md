---
phase: 07-security-hardening-for-live-runtime
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/auth/jwt-verifier.ts
  - src/auth/auth-guard.ts
  - src/auth/websocket-auth.ts
  - tests/unit/auth-guard.test.ts
  - tests/unit/websocket-auth.test.ts
  - tests/integration/protected-route-auth.test.ts
  - package.json
  - package-lock.json
  - src/app/secret-store.ts
  - src/app/config.ts
  - tests/unit/secret-store.test.ts
  - tests/unit/config.test.ts
  - src/realtime/message-relay-publisher.ts
  - src/realtime/group-relay-publisher.ts
  - src/realtime/trust-change-publisher.ts
  - tests/unit/message-relay-publisher.test.ts
  - tests/unit/group-relay-publisher.test.ts
  - tests/unit/trust-change-publisher.test.ts
autonomous: true
requirements:
  - SEC-02
must_haves:
  truths:
    - "Protected HTTP and WebSocket routes accept valid access or ID tokens according to the route matrix."
    - "Production WebSocket connects reject query-token fallback and require the Authorization header."
    - "Missing secret material fails closed instead of falling back to insecure defaults."
    - "Realtime warning and error logs preserve requestId or event metadata but never expose raw tokens or raw user/device identifiers."
  artifacts:
    - path: src/auth/jwt-verifier.ts
      provides: "Token verification helper with route-aware token-use policy"
    - path: src/auth/websocket-auth.ts
      provides: "Production-safe WebSocket auth source selection"
    - path: src/app/secret-store.ts
      provides: "Secrets Manager-backed secret resolution helper"
    - path: src/realtime/message-relay-publisher.ts
      provides: "Redacted delivery-failure logging"
  key_links:
    - from: src/auth/jwt-verifier.ts
      to: src/auth/auth-guard.ts
      via: "Shared Cognito claim verification"
      pattern: "AUTH_TOKEN_INVALID_CLAIMS"
    - from: src/auth/websocket-auth.ts
      to: src/handlers/ws/connect.ts
      via: "Connect-time token source selection"
      pattern: "Authorization"
    - from: src/app/secret-store.ts
      to: src/app/config.ts
      via: "Fail-closed secret lookup"
      pattern: "GetSecretValue"
    - from: src/realtime/message-relay-publisher.ts
      to: console.warn
      via: "Redacted log payloads"
      pattern: "userId|deviceId|connectionId"
---

<objective>
Enforce production-safe auth, secret, and log defaults in the runtime.

Purpose: Keep live routes secure even when token sources vary, prevent silent secret fallback, and stop sensitive metadata from leaking into logs.
Output: Updated auth verification, Secrets Manager-backed secret resolution, and redacted realtime log output with tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From src/auth/auth-guard.ts:
```typescript
export async function requireAuth(
  authorizationHeader?: string | null,
): Promise<AuthenticatedUser>
```

From src/auth/websocket-auth.ts:
```typescript
export async function extractWebSocketContext(
  event: WebSocketConnectEvent,
): Promise<WebSocketConnectionContext>
```

From src/realtime/message-relay-publisher.ts:
```typescript
export async function relayDirectMessage(
  recipientUserId: string,
  recipientDeviceId: string,
  event: DirectMessageEvent,
): Promise<DeliveryState>
```

From src/realtime/trust-change-publisher.ts:
```typescript
export async function publishTrustChange(userId: string, event: TrustChangeEvent): Promise<void>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make auth verification route-aware and production-safe</name>
  <files>src/auth/jwt-verifier.ts, src/auth/auth-guard.ts, src/auth/websocket-auth.ts, tests/unit/auth-guard.test.ts, tests/unit/websocket-auth.test.ts, tests/integration/protected-route-auth.test.ts</files>
  <action>Extend JWT verification so protected routes accept both access and ID tokens while preserving the stable AUTH_TOKEN_* machine codes and generic human-facing message. Keep the WebSocket connect path strict in production by requiring an Authorization header there and only allowing query-token fallback outside production, while leaving the existing trusted-device checks in place for sensitive routes.</action>
  <verify>npm run test:auth</verify>
  <done>Protected routes continue to authenticate correctly with the expected machine-readable failures, and production WebSocket connects no longer accept query-token fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Add fail-closed secret resolution for runtime config</name>
  <files>package.json, package-lock.json, src/app/secret-store.ts, src/app/config.ts, tests/unit/secret-store.test.ts, tests/unit/config.test.ts</files>
  <action>Add a cached Secrets Manager-backed secret resolver that reads named or ARN-based secrets, throws when required values are missing or empty, and keeps non-sensitive runtime config in normal env/parameter inputs. Wire the config layer to use the helper for any secret-backed values introduced in this phase, with no insecure fallback defaults.</action>
  <verify>npm run build && npm test -- tests/unit/secret-store.test.ts tests/unit/config.test.ts</verify>
  <done>Secret-backed values resolve only through the approved secret path and missing secrets fail fast.</done>
</task>

<task type="auto">
  <name>Task 3: Redact sensitive metadata in realtime warning logs</name>
  <files>src/realtime/message-relay-publisher.ts, src/realtime/group-relay-publisher.ts, src/realtime/trust-change-publisher.ts, tests/unit/message-relay-publisher.test.ts, tests/unit/group-relay-publisher.test.ts, tests/unit/trust-change-publisher.test.ts</files>
  <action>Replace raw identifier logging with stable redaction or hashing output, keep requestId, error code, route or eventType, and timestamp in the emitted metadata, and ensure raw Authorization headers, query tokens, and message bodies never appear in warning or error logs.</action>
  <verify>npm test -- tests/unit/message-relay-publisher.test.ts tests/unit/group-relay-publisher.test.ts tests/unit/trust-change-publisher.test.ts</verify>
  <done>Realtime logs preserve triage data but do not leak raw tokens or raw user/device identifiers.</done>
</task>

</tasks>

<verification>
The phase is complete when protected-route auth behavior, secret handling, and realtime log output all satisfy the production-safe defaults from the phase context and the targeted tests pass.
</verification>

<success_criteria>
Protected routes accept the correct token types, production WebSocket auth is header-only, secret lookups fail closed, and realtime logs no longer expose sensitive identifiers or token material.
</success_criteria>

<output>
After completion, create `.planning/phases/07-security-hardening-for-live-runtime/07-02-SUMMARY.md`
</output>